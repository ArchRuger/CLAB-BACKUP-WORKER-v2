# Evidence matrix

One row per acceptance check and image. **PASS / FAIL / BLOCKED / NOT RUN.** "Build" is the manager
build that produced the evidence; a row proven on an earlier build is rerun when a later change
touches its path. Layers: *product* = through the running manager's API or browser; *driver* = the
driver module called directly against the node; *device* = raw CLI through `tools/nodecli.py`;
*unit* = scripted fake device or fake connector. Raw transcripts are outside Git
(`~/research/multi-platform-restore/raw/`); the files named here are sanitized.

Images: cEOS `n24l/ceos:4.35.0F`, cJunosEvolved `n24l/cjunosevolved:26.2R1.7-EVO`, vJunos-switch
`n24l/vjunos-switch:23.2R1.14`, XRv9k `n24l/cisco_xrv9k:24.3.1` (IDs and reported NOS versions in
[PICKUP](../PICKUP.md)).

## How to read the builds

Several working-tree builds carried the release number 1.30.27. `-wt1` = deployed 2026-09-20 23:30 UTC (first cEOS
build); `-wt2` = 2026-09-21 00:16 UTC (token identity, `commit check` confirmation, orphan cleanup, submit probe; a
rebuild at 00:21 changed one sentence of the page). From `18-*` on every evidence file names its build itself
(`build`: running image id, container start, checkout commit, number of uncommitted application files), because an
independent audit of this matrix (`PICKUP.md`, "Evidence audit") found that prose was the only place the builds were told
apart. The same audit is why the newer files embed the devices' own answers (`tools/readback.py`: booleans, counts and boot
identity, never configuration text) and an independent whole-configuration comparison; where an older row rests on raw
transcripts outside Git or on the manager's own report, the row says so.

## A. Complete replacement through the product

| Check | cEOS 4.35.0F | cJunosEvolved 26.2R1.7-EVO | vJunos-switch 23.2R1.14 | XRv9k 24.3.1 |
|---|---|---|---|---|
| A1 configuration A (interfaces, loopback, OSPF, static route, prefix list or prefix-set), data plane, boot identity | PASS reachability and boot identity `00-square-baseline.json`; A itself active: the readbacks inside `50-*`, `60-*` (markers + whole-configuration comparison with the saved files) | PASS same | PASS same | PASS same |
| A2 A saved through the manager's normal workflow, restore-grade artifact, integrity | PASS: backups `64bb697e…`, `177b0f23…`, `61cfe431…` and the *Save progress* saves `9d5906d`, `366c21f` were real restore sources. Integrity: Git and folder sources per file against the manifest (size, SHA-256); a backup against the digest recorded when it was stored (captures older than 1.30.28 carry none). Unit-tested with tampered files; the pipeline with real Ansible; no live tamper test | PASS same | PASS same | PASS `.xrcfg` in backup `61cfe431…` and save `366c21f`; the manager's normalised capture equals the driver's own capture line for line (`xr-live-facts.md`, integration check) |
| A3 drift to B active: a value modified, a statement removed, B-only stanzas added | PASS `device_readback_before` = B in `50-*`, `53-*`, `60-*` | PASS `50-*`, `54-*`, `55-*`, `60-*` | PASS `50-*`, `54-*`, `55-*`, `60-*` | PASS `40-*`, `44-*-1`, `45-*`, `50-*`, `60-*` |
| A4 restore A from the running UI, every source type the page offers | PASS folder `14-*`, `53-*`, `50-*`; commit-pinned Git version (the row's View dialog) `54-*`; backup (API only) `10-*`, `13-*`, `60-*` | PASS folder `31-*`, `55-*`, `50-*`; Git version `54-*`; backup `56-*`, `57-*`, `60-*` | PASS folder `21-*`, `55-*`, `50-*`; Git version `54-*`; backup `56-*`, `60-*` | PASS folder `50-*` (browser), `44-*` (API); Git version from the browser at 390 px `45-xr-browser-git-version-narrow.json`; backup `40-*`, `60-*` |
| A5 independent readback through a fresh device connection; whole configuration compared | PASS `50-*`, `53-*`, `54-*`, `60-*`: markers (value restored, statement back, B-only gone) and `tools/readback.py`'s own comparator against the saved files: 0 missing, 0 extra; the manager's `verified` 0/0 agrees | PASS same files + `55-*`…`57-*`; `57-*` reports the one tolerated difference (the synthesised root-authentication) | PASS same files + `55-*`, `56-*` | PASS `44-*`, `45-*`, `50-*`, `60-*` |
| A6 management reconnects, links recover, no NOS restart; measured interruption (`60-interruption-<node>.json`: a restore that changed configuration, B before and A after by the device, 0.2 s probes from the VM and from cEOS's namespace, 1 s CLI pings where the source is another router) | PASS 0 lost on management, on both edges from the neighbours' side and on a path that transits cEOS; boot identity (age of PID 1) unbroken | PASS 0 lost: management, both edges, a transit path; longest gap 0.24 s; `System booted` equal | PASS 0 lost: management, both edges, a transit path; `System booted` equal | PASS 0 lost: management, both edges, a transit path; longest gap 0.22 s; uptime unbroken |
| A7 repeat cycle, A onto A, post-restore backup as a source, nothing pending, nonselected nodes unchanged | PASS `12-ceos-a7-1…3` (`-wt1`); nonselected `53-*` | PASS `56-*` (A onto A `no_op` true; restore from a post-restore backup) | PASS `56-*`; bystander in `53-*` | PASS `44-xr-a7-1…3`: recovery over B, A onto A (`no_op` true; IOS XR creates no trial for it), and immediately again: not blocked by the manager's own session, session table empty afterwards; untouched as the refused node of `51-*` |

The drift changes a description, a static route, a prefix list or prefix-set and a VLAN or loopback. A restore that
changes addressing or routing on a probed path interrupts it by as much as that change does.

## B. Failure handling and recovery

| Check | cEOS | cJunosEvolved | vJunos-switch | XRv9k |
|---|---|---|---|---|
| B1 invalid syntax / commit rejection | PASS driver+device `12-ceos-b1-…json` (`-wt1`) | PASS driver layer `19-06-check6-driver-b1.json`: a candidate the parser rejects and one that `commit check` rejects: RestoreError without device text, candidate discarded, exclusive session closed, nothing pending, configuration unchanged | PASS same file | driver layer `xr-live-facts.md` §5: a bad line is rejected, nothing of the candidate active, no session left. A commit-time-only semantic rejection could NOT be produced on this image (duplicate addresses and an undefined prefix-set were accepted); the driver's check for it is unit-tested only |
| B2 missing / truncated / wrong-format / legacy artifact, integrity mismatch, wrong node | PASS `12-ceos-b2-…json`; legacy rows `57-*`; wrong node mapping `17-08-*`; unit PASS incl. tampered files | unit PASS; `17-08-*` | unit PASS; `17-08-*` | legacy save in the browser `18-browser-disabled-row-legacy-save.json` ("made before this kind of device could be restored"); a candidate with a `banner` is refused before the device (unit); unit PASS |
| B3 unreachable node / rejected credentials, at the review and at application | PASS review `12-ceos-b3-…json`; application `19-01-check1-ceos-*` (cut after the submit: `failed`, not changed, no session left, recovery verified); credentials `19-02-*`: the review says the login was rejected, at application the node is not changed and the device logged no hammering. The reason shown at application was the generic backup failure in that run; since then the safety backup's failure is classified ("the device rejected the login"), unit-tested, not rerun live | PASS application `19-01-check1-cjunosevolved-*`; credentials: shared path, cEOS run | PASS `19-01-check1-vjunos-switch-*` | PASS application `46-xr-b3-unreachable-at-application.json` (`failed`, "not changed", device still B, nothing pending); at the review a refused connection is retried (the first XR run showed why: `40-*` preflight row) |
| B4 management lost after arming: timer observed independently, read-back by the manager, later restore works | PASS `17-02-*`: EOS reverted at +126 s (seen through `docker exec … Cli`), manager read B back → `rolled_back`, no own session left, recovery verified | PASS `17-04-*attempt4*`: rollback time from the device's commit log; recovery verified | PASS `17-05-*` + recovery | PASS `41-xr-b4-management-cut-after-arming.json`: cut when the fresh connection opened (this also kills the held arming session), `rolled_back` after the manager read B back, device: B, nothing pending, uptime unbroken; recovery `44-*-1` |
| B5 manager restart during a restore; SSH loss after activation | PASS `17-03-*`: settled by the restart re-check, job recomputed, nothing re-applied | PASS `17-06-*`: the pending change found under the job's own token, confirmed, `verified` | unit PASS (shared logic; the Junos mechanism is the one proven on Evolved) | PASS both orders: `42-*` the confirmation got through just before the restart → re-check `verified` (device: A, no rollback in the commit list); `43-*` management cut before the confirmation and the manager restarted → the held session is gone, the node rolls back, re-check `rolled_back` (device: B). The harness stopped polling early in both; the settled job was read from the manager and is recorded as `lead_note` |
| B6 foreign session / candidate / pending confirmation; double submit; contention | PASS `12-ceos-b6a…`, `13-ceos-orphan-cleanup-restore.json`, `13-ceos-submit-refused-foreign-timer.json`, double submit `10-*`; contention with a backup `12-ceos-b7-*`, with a Save and with a lab operation `19-04-*` (one shared guard) | PASS at the API `19-03-check3a-evo-*` (foreign `commit confirmed`: row ineligible, HTTP 409 twice, nothing created, it rolled back by itself), `19-03-check3b-evo-*` (bystander's uncommitted edit: ineligible, 409, still in the candidate, never activated); driver layer `30-*` | PASS `19-03-check3a-vjunos-*`, `19-03-check3b-vjunos-*`; driver layer `20-*` | PASS `47-*`: somebody's open configuration session → "A configuration session is open on this node", a foreign pending change → "Another change is waiting for confirmation"; both: HTTP 409 twice, no job, no backup; mid-job in `51-*` |
| B7 applied / verified / mismatched / rolled back / uncertain told apart, API and page | PASS a real `verify_mismatch` `19-05-*` (one statement added in the gap before the verification: 1 unexpected statement, masked sample without secrets, job `needs_attention`); real `rolled_back` jobs in the browser `58-*`; unit PASS for the rest | `58-*` job 2; unit | `58-*` job 3; unit | the refused node of a `partial` job in the browser `52-*`; unit |

## C. Persistence, browser, mixed platforms

| Check | Result |
|---|---|
| C1 confirmed restore survives a controlled NOS restart | PASS on all four, build 1.30.29 (`63-persistence-<node>.json`): the last configuration change on every node was a confirmed restore from the browser (`61-*`); then the NOS was restarted the normal way (Junos `request system reboot`, IOS XR `reload`: "User initiated graceful reload", cEOS `containerlab restart --node ceos`), the restart proven from the NOS itself (uptime fell), management and both edges waited for, and the configuration compared by the tool's own comparator: cEOS 47, cJunosEvolved 23, vJunos-switch 25, XRv9k 51 statements, 0 lost, 0 new, order unchanged. The cEOS result is what the driver's `write memory` after the confirmation is for. Square healthy afterwards `64-*`; a real all-four restore after the restarts `65-*` (B before, A after, 0/0 on all four). A containerlab redeploy was not used anywhere |
| C2 real browser against the deployed manager and the real nodes | PASS: fresh assets, device-type labels, selection, acknowledgement refused without the tick, Space and Enter by keyboard, progress, reopen from the banner while running, reload mid-job, result, both backups under Details; disabled rows with their reasons `18-*`; restore → backup from the page → restore again in one session at 390 px `55-*`; both source types; 1366, 768 and 390 px. A check that could not be exercised is "n/a", never a pass. At 390 px the Progress tab itself is 431 px wide before any dialog; the restore dialogs fit and add nothing |
| C3 all four nodes from one saved state | PASS `50-all-four-browser-desktop.json`, and again on the released 1.30.29 build with no uncommitted file `61-all-four-browser-on-the-released-build.json`: all four drifted, restored from one folder save through the browser, 28 checks, the devices say B before and A after, 0 missing / 0 extra on all four by the independent comparator, no restart. Nodes are applied one after another |
| C4 mixed selection with one controlled failure | PASS `51-mixed-four-one-controlled-failure.json`: a foreign timed change armed on XRv9k while the job worked on the first nodes → three `verified`, XRv9k `failed` "not changed" with the true reason, job `partial`, the foreign change left alone and rolled back by itself, recovery verified; what the page shows `52-*` ("Configuration replaced on 3 devices. 1 device was not changed."). The file notes that the readback tool's XR pending detection was wrong at the time of that run |
