# Setup Script Cleanup Log: requirement map

Source: the user's PDF *Setup Script Cleanup Log* (25 pages, 2026-09-23; text and every rendered page
were read). Columns: PDF page → requirement → implementation → acceptance evidence → status.
Status vocabulary: `todo`, `in progress`, `done` (evidence in `VALIDATION.md`), `blocked` (reason),
`already correct` (verified, regression-protected).

| Page | ID | Requirement (as read from the PDF, incl. screenshot detail) | Implementation | Evidence | Status |
|---|---|---|---|---|---|
| 1 | A1 | "make sure all junos config backups save as .cfg". Screenshot p.21 shows Git-saved Junos files as `NAME.jcfg` and `NAME.set`. Downloads already end in `.cfg` for Junos (NODE-FEATURES contract). | Git snapshot human backup for Junos kinds becomes `<label>.cfg` (display-set content unchanged); the hierarchical restore artifact stays a machine file (`.jcfg`, referenced by `manifest.json`, never by extension); old `.set` snapshots stay readable through their manifest. | | todo |
| 1 | A2 | Screenshot: bind/port menu, lab-operation menu, VS Code access menu, "Back up and disable obsolete installation-media APT entries?", "Proceed with this plan?" are always accepted; do not present them. | `install-manager.py`: standard path takes the defaults silently (all interfaces/8081 when no `.env`, reviewed operations, VS Code access for the invoking user, APT media backup, plan printed and started); existing `.env`/registrations retained; `--advanced` restores the questions (documented). | | todo |
| 1–2 | A3 | dpkg lock held by unattended-upgrades: give a command that adapts to the live PID; offer to run it automatically; the recovery menu. | New `deploy/apt_lock.py` (stdlib): finds the current holder from `/proc`, bounded wait, optional timer pause with state restore, never kills or deletes locks; installer detects the lock error, prints the copyable helper command and offers automatic wait then retry. | | todo |
| 2–6 | A4 | Condense Git onboarding: GitHub CLI install and login without yes/no, no subfolder question (repository root), registration defaults to yes and completes automatically. | `git-onboard.py`: gh installed when missing, login run headless through a pipe (no gh prompt), subfolder prompt removed (prefix = root for new registrations; existing prefixes kept), registration automatic once checks pass; clone/existing/owner choices kept. | | todo |
| 2, 5–7 | A5 | Print `https://github.com/login/device/select_account` next to the device code; on success print completion info and exit to the shell instead of the setup menu. | `git-onboard.py` prints the hint right after gh's device URL; `install-manager.py` exits 0 after a successful chosen path; failure/cancel keeps the recovery/menu path. | | todo |
| 7 | A6 | Add lazydocker; `export PATH=$PATH:$HOME/.local/bin` in `.bashrc` ("Don't forget this part"). | Installer step (as the ordinary user): upstream release tarball for the machine architecture into `~/.local/bin`, one guarded PATH line in `~/.bashrc` (idempotent), `lazydocker --version` checked. | | todo |
| 7–8 | A7 | VM connection dialog: address, port, username, password, inspection method, auto-check should not need re-entry after setup. | Setup writes a one-time bootstrap seed (root/manager-uid only) that the manager consumes into the encrypted store: fields prefilled, password stored; the student confirms the connection once (explicit first-use trust kept; installer-recorded fingerprint shown for comparison). | | todo |
| 8–9 | B1 | "Upload a lab file from this computer": optional annotations JSON chooser below the topology input. | Second file input (`.annotations.json`, size-limited, validated JSON) carried through the reviewed `create` as the companion file; topology-only upload unchanged. | | todo |
| 9–11 | B2 | "Edit visually…" misleading → "Open in Lab Builder…"; "← My labs" must return to the open Topology file dialog with its VM path and content, not to empty My labs. | Label renamed; the dialog state (path, source) is kept in sessionStorage and restored by the router when returning; changed content re-read. | | todo |
| 11 | B3 | Preview topology dialog about the size of the red box (near full viewport); remove "Wiring from the topology file; device positions from its saved map file." | Preview dialog sized to the viewport with margins, fit on open; caption removed. | | todo |
| 12–13 | B4 | Deploy review: remove the orange "Runs this trusted topology with host privileges…" box and "Runs on the lab VM…"; show the command directly (no Technical details toggle); open the operation output automatically after Start. | Review dialog shows target and command inline; on confirm the output dialog opens at once; a deliberate close is not reopened by polling. | | todo |
| 13 | B5 | "All notices should be hideable to some capacity" (screenshot: "3 devices need login credentials…"). | Lab/home banners get a close control; dismissal is per notice key and lab, survives rerender, resets when the notice text materially changes; no dismissal acknowledges an operation or removes a confirmation. | | todo |
| 13 | B6 | network-multitool default credentials are public; bake them in so the stock image does not warn. | Documented default for nodes whose image is `ghcr.io/srl-labs/network-multitool` (kind `linux`), after profile and inventory; other Linux images unchanged. | | todo |
| 13–16 | B7 | Remove "Details / Lines show how the lab is wired, not whether links are up."; make the right-hand Devices column scroll on its own. | Details element removed (tests rewritten); the device rail becomes a bounded, independently scrollable column at desktop widths. | | todo |
| 16–17 | C1 | New lab: remove "Start from" and "Device type for the starter"; always a blank canvas. | Dialog reduced to name and folder; starter code and tests removed/rewritten. | | todo |
| 17 | C2 | Tiny grey pill beside "Lab builder" with no draft. | `#builder-status` is hidden when there is no draft; keeps its accessible text otherwise. | | todo |
| 17 | C3 | Drag/drop a `clab.yml` + `annotations.json` pair into the builder. | Drop target on the welcome page and canvas plus a file picker; pair matched by name, order-independent, validated; draft replacement confirmed. | | todo |
| 17–18 | C4 | View YAML → editable YAML with the map reflecting valid edits and vice versa. | Editor panel using the engine's `setYamlContent` command; visual edits refresh the text; invalid YAML shows diagnostics and keeps the last valid graph. | | todo |
| 18 | D1 | Right-click a link: `ge-0/0/35` should map automatically to the capture interface. | Platform-aware endpoint resolution from the topology's link definition and the runtime interface list; manual selection kept for unresolved cases with a reason. | | todo |
| 19 | D2 | A visible Test login / Refresh control by the Devices heading; hover explains; disabled Open CLI points to it. | Rail control runs the real login test for the lab's devices with bounded concurrency; per-node results; tooltip and disabled reasons updated. | | todo |
| 19–20 | E1 | "Saved on this VM" is not descriptive: label saves (also the commit message). | Save dialog requires a label (validated, escaped); used in pending list, history, job detail and commit message; unlabeled old saves fall back to the date. | | todo |
| 20–21 | E2 | "Saves waiting to be uploaded" stays open after "View configuration backup". | Navigation closes the stale dialog layers and restores focus. | | todo |
| 21–22 | E3 | Show the full save destination (repository · branch · path), not just "Latest"/"latest". | Review, completion and details show `<repo> · <branch> · <path>` from the frozen job destination; local checkout path in details. | | todo |
| 22 | E4 | Side-by-side full texts → real diff (highlighted insertions/deletions, context, line numbers). | Server-side unified diff per file (difflib) rendered with line numbers and colours; escaped; narrow layout. | | todo |
| 22–23 | E5 | Restore dialog: rows with differences should expand to the actual saved-vs-running comparison before submitting. | Preflight returns the per-device diff; rows expand to the rendered diff; states already-matches / differences / unsupported / failed distinct. | | todo |
| 23–25 | E6 | Show the sequence of stages and the current one; success green with text; no "Backing up…" before it starts. | Per-device stage list derived from backend states with timestamps; queued shown as queued; distinct failed/skipped/matched/uncertain/rolled back. | | todo |
| 24–25 | E7 | Why 1:1? Run devices in parallel. | Investigated (Opus review); bounded worker pool with per-device atomic status updates where the transaction contract permits; evidence of overlap. | | todo |
| — | F1 | Stable Latest destination (regression). | Verified again with live saves. | | todo |
| — | F2 | Restore from any manifest-bearing folder (regression). | Verified again with live folders. | | todo |
