# Parity mapping — operations module (operations.js + its entry points)

Source inventory: `inventory/operations.md`. Spec under review: `DESIGN-SPEC.md`. Screenshots consulted: `shots/before/23-lab-actions-menu`, `18-destroy-dialog`, `10-inspect-all`, `09-operation-history`, `08-deploy-new-lab`, `01-workspace-topology--1366x768`. Test contracts checked against `clab-backup-ui/tests/test_operations_ui.js` (12 tests) and `inventory/tests.md` §9.

Change types: unchanged · moved · renamed · moved+renamed · reorganized · behaviour-changed · UNMAPPED. "Gn" in notes refers to the spec-gap list at the end.

Totals: UNMAPPED 2 · behaviour-changed 1 · moved 4 · moved+renamed 22 · renamed 41 · reorganized 2 · unchanged 10 (rows: 82; spec gaps: 20).


## Capability mapping

| id | capability (current label) | current location | new location (spec) | new student-facing label / copy | change | note |
|---|---|---|---|---|---|---|
| OPS-01 | opDialog shell (eyebrow "NODE MANAGER", x close, title, .form-error alert) | Every operations dialog (also used by diagram-editor.js and git-progress.js) | Unchanged component: dialog.operations-dialog with .dialog-head per spec §3 "Dialog" | Eyebrow per dialog: "Confirm" (review), "Lab operation" (output), "History", "Deploy" (browser), "Topology file" (editor), "Download" (clone/popular), "Telemetry", "All lab operations"; close keeps aria-label "Close" | renamed | Signature opDialog(id,title,body) is a cross-file contract (tests.md §0); add an optional 4th argument eyebrow defaulting to "Lab" so diagram-editor.js and git-progress.js callers keep working. Spec gives no eyebrow text (gap G17). |
| OPS-02 | opTask (disables enabled buttons while a task runs; error to .form-error or toast when dialog is null) | All operations dialogs and the header quick actions | Unchanged helper | — | unchanged | Also disables the x close button during a task (keep). Errors from header/menu actions go to the 5 s toast, which cannot carry a "Details" layer; route lifecycle-action failures into #lab-banner (danger variant) with the backend message under Details (see G4). |
| OPS-03 | "Lab actions ▾" button opening openLabOperations() (the two-column operations dialog) | Lab page › page heading › #lab-actions (JS-injected before #import-top; hidden without a lab) | Lab workspace › header › Lab actions ▾ › Lab group › "All lab operations…" (#lab-actions, spec §1.3); also Advanced › Lab operations › [All lab operations…] | All lab operations… | moved+renamed | operations.js still injects #lab-actions before #import-top unconditionally; if index.html declares the menu item with the same id there are two elements and $() binds the wrong one (G1). |
| OPS-04 | Operations dialog phase 1/2: "Checking installed VM commands…", .op-path line, capability-error .op-notice | Lab actions dialog (#lab-operations-dialog) | All lab operations dialog (same id), opened from OPS-03 | "Checking what this VM can do…"; path line "Topology file on the VM: <path>"; no path → "This lab has no topology file on the VM yet. Import the lab's files (Advanced › Lab source) to enable these actions."; error → "Couldn't check the VM's commands, so every action is shown; some may fail. Details: <message>" | renamed | Behaviour kept: when capabilities fail nothing is disabled and destroy still sends cleanup:true (opDestroyOptions). Spec §4 says only "dialog copy" (G18). |
| OPS-05 | Deploy lab (action deploy) | Lab actions dialog › Deployment & configuration | Lab actions ▾ › Lifecycle › #lab-start when status is "Not deployed" (startAction=deploy); All lab operations dialog › Deployment; Home lab card [Start lab] proxy | "Start lab" in the menu/card; dialog button "Deploy lab" with hint "creates and starts the devices" | moved+renamed | Spec uses "Start lab" for both deploy and start (§1.2, §1.3); the dialog keeps the containerlab word so instructors recognise the operation. Review copy OPS-36. |
| OPS-06 | Redeploy (action redeploy) | Lab actions dialog › Deployment & configuration | Lab actions ▾ › Lifecycle › "Redeploy lab" ([data-op-action=redeploy]); All lab operations dialog › Deployment | Redeploy lab | moved+renamed | The new menu item has no gating rule in the spec: the dialog gates by opCaps.actions[a].available (fetched lazily in openLabOperations) and the quick actions by opQuickActions; a menu item wired straight to opReview can be clicked while the VM is disconnected or a job is running (G4). Confirmation copy per spec §1.6. |
| OPS-07 | Apply topology (action apply; review shows the YAML diff) | Lab actions dialog › Deployment & configuration | All lab operations dialog › Deployment | Apply topology changes | moved+renamed | Reachable only via Lab actions ▾ › All lab operations… (2 clicks): acceptable as an advanced action. Review copy is not in the spec (G5, OPS-37). |
| OPS-08 | Start lab nodes (action start) | Lab actions dialog › Deployment & configuration; deployment bar #lab-start when Stopped / Partially running | Lab actions ▾ › Lifecycle › #lab-start (label "Start stopped devices" when startAction=start); All lab operations dialog › Deployment | Start stopped devices | moved+renamed | Spec says the item reads "Start stopped devices" "when the lab is running", but opQuickActions disables Start when status==="Running"; the relabel only applies to Stopped / Partially running (G2). |
| OPS-09 | Stop lab nodes (action stop) | Lab actions dialog › Deployment & configuration | Lab actions ▾ › Lifecycle › "Stop devices" ([data-op-action=stop]); All lab operations dialog › Deployment | Stop devices | moved+renamed | Gating gap G4 (should require status Running or Partially running, VM connected, not busy, capability available). Confirmation copy per spec §1.6. |
| OPS-10 | Restart lab nodes (action restart) | Lab actions dialog › Deployment & configuration | Lab actions ▾ › Lifecycle › "Restart devices" ([data-op-action=restart]); All lab operations dialog › Deployment | Restart devices | moved+renamed | G4. Spec §1.6 gives Stop copy only; proposed: "Restart devices?" — "Devices restart from their startup configuration; unsaved running-config changes may be lost on some platforms. Open CLI sessions disconnect." [Restart devices]. |
| OPS-11 | Inspect lab (action inspect → containerlab inspect table for this lab) | Lab actions dialog › Deployment & configuration | All lab operations dialog › Deployment | Show running devices | moved+renamed | Read-only but the helper token forces a confirm; spec §0.6 fixes only inspect-all. Same treatment: title "Refresh the device list for <lab>", body "Reads the current state of this lab's devices from the VM. Nothing is changed.", confirm "Show devices", no notices or "n affected" line (G5). |
| OPS-12 | Save configurations (clab) (action save = containerlab save) | Lab actions dialog › Deployment & configuration | All lab operations dialog › Deployment | Save device configurations on the VM | moved+renamed | Must not be confused with Save progress: hint under the button "Uses containerlab's own save for supported device types. Your progress snapshots are separate (Progress tab)." Review copy OPS-38. |
| OPS-13 | Destroy deployment (action destroy; options {cleanup:true} unless caps.destroy.cleanup===false) | Lab actions dialog › Deployment & configuration; deployment bar #lab-destroy | Lab actions ▾ › Danger › #lab-destroy "Destroy lab…"; All lab operations dialog › Danger (danger-outline) | Destroy lab… | moved+renamed | Confirmation per spec §1.6 (includes the cleanup sentence). opDestroyOptions unchanged; test 11 asserts data-op-action="destroy" and data-op-options="{&quot;cleanup&quot;:true}" — keep the attributes. |
| OPS-14 | "Deploy lab + cleanup" / "Redeploy + cleanup" variants ({cleanup:true}; rendered only when caps.actions[a].cleanup) | Lab actions dialog › Deployment & configuration (after destroy) | Lab actions ▾ › Danger › "Redeploy with cleanup…"; both variants in All lab operations dialog › Danger | Redeploy and clear the lab folder… / Deploy lab and clear the lab folder | moved+renamed | Spec writes [data-op-action=redeploy-cleanup]; "redeploy-cleanup" is not a backend action name — POST /operations/preview would reject it. The item must be data-op-action="redeploy" with data-op-options={"cleanup":true} (G4). Show only when opCaps.actions.redeploy.cleanup, as the dialog does today. |
| OPS-15 | Capability gating in opCommand: disabled button + <small>Unavailable on this VM</small> | Lab actions dialog buttons; browser Clone/Popular | Same, plus the new Lab actions ▾ lifecycle items | Not available on this VM — see Diagnostics | renamed | The capability API gives no reason, so point to Diagnostics (the debug page). Keep data-op-action / data-op-options attributes (test 11). Menu items must apply the same rule (G4, G16). |
| OPS-16 | SSH all nodes ↗ (opNewTab({mode:"ssh"}) → workspace.html SSH launcher) | Lab actions dialog › Workspace & access; topology heading #map-ssh-all | Topology › map tools › More ▾ › "Open all CLIs ↗" (#map-ssh-all); Tools › "Open all CLIs" card; All lab operations dialog › Lab tools | Open all CLIs ↗ | moved+renamed | Same function everywhere; the popup-blocked fallback is OPS-75; the launcher page copy is OPS-76 (unmapped). |
| OPS-17 | Favorite lab / Remove favorite (PUT /labs/{id}/operations-settings {favorite}; app.js sorts favourites first with a ★) | Lab actions dialog › Workspace & access | All lab operations dialog › Lab tools (plus proposed star toggle on the Home lab card) | Add to favourites / Remove from favourites | moved+renamed | Spec §1.2 orders favourites first on Home but gives no way to set one there; the only toggle is buried three clicks deep (G13). Behaviour (refresh + close) unchanged. |
| OPS-18 | Edit topology diagram (opLayout → editDiagram) | Lab actions dialog › Workspace & access; map tools #map-edit | Topology › map tools #map-edit "Edit diagram"; Lab actions ▾ › Lab › Edit diagram; All lab operations dialog › Lab tools | Edit diagram | moved | Same function; three entry points. |
| OPS-19 | Telemetry settings… (closes the dialog, opens openTelemetrySettings(id)) | Lab actions dialog › Workspace & access | Tools › Telemetry card › [Telemetry settings…]; Lab actions ▾ › Lab › Telemetry settings…; All lab operations dialog › Lab tools | Telemetry settings… | moved | Dialog contents are OPS-23…31. |
| OPS-20 | Operation history for this lab (opHistory(id)) | Lab actions dialog › Workspace & access | Lab actions ▾ › Lab › Operation history… (opHistory(lab.id)); Advanced › Lab operations › [Operation history…] | Operation history… | moved | Spec says "filtered to the lab if the dialog supports it" — it does (labId argument); the spec should state the filtered call explicitly. |
| OPS-21 | Delete undeployed VM YAML (action delete; capability-gated) | Lab actions dialog › Workspace & access | All lab operations dialog › Danger (below Destroy, danger-outline) | Delete the topology file from the VM… | moved+renamed | Review copy (G5): "Delete <lab>'s topology file from the VM?" — "<path> is deleted after a recovery copy is kept. The lab stays in My labs and your saved progress is untouched. Only possible while the lab is not running." [Delete file]. |
| OPS-22 | Dialog help paragraph (destroy --cleanup, redeploy variants, containerlab save) | Lab actions dialog › Deployment & configuration footer | All lab operations dialog › Deployment footer | "Destroy removes the running devices and the lab's generated folder on the VM. Redeploy keeps that folder unless you choose the 'clear the lab folder' variant. 'Save device configurations' uses containerlab's own save (supported device types only); your Save progress snapshots are separate." | renamed | — |
| OPS-23 | Telemetry summary sentence (5 cases: disabled, unlinked, undecided, off, on with counts) | Telemetry settings dialog | Tools › Telemetry card status line (short form from lab.telemetry.status) + Telemetry settings dialog (full sentence) | (1) "Telemetry is not installed on this VM." (backend `unavailable` text under Details) (2) "Telemetry only works for labs running on the VM; this lab isn't linked to a running lab yet." (3) "Telemetry hasn't been set up for this lab. Nothing changes on any device until you turn it on." (4) "Telemetry is off for this lab, so the network dashboard shows nothing for it." (5) "Telemetry is on: {total} supported devices · {n} streaming · {n} stale · {n} waiting · {n} configuring · {n} connecting · {n} failed · {n} unsupported. Open the network dashboard to see the data." | renamed | Test 10 asserts the current sentences (/Automatic telemetry is on: …/, /saved before automatic telemetry existed/, /pygnmi is not installed/); regexes change with the copy (G14). Case (1) must still surface data.unavailable verbatim (Details). |
| OPS-24 | Failed/stale device list "<strong>name</strong> (state word): message" (teleStateLabels) | Telemetry settings dialog | Telemetry settings dialog › "Devices that need attention" | Heading "Devices that need attention:"; rows unchanged | renamed | Test 10 asserts <strong>r2</strong> (failed): … — keep the row markup and the state words. |
| OPS-25 | Grafana status line (#tele-grafana, 6 sentences) + "Stop Grafana now" (#tele-grafana-stop → POST grafana/stop) | Telemetry settings dialog | Telemetry settings dialog › Dashboard | "Dashboard: running (port {port}); stops automatically after {n} minutes without a viewer." / "…; automatic stop is off." / "Dashboard: not installed on this VM." / "Dashboard: stopped; it starts when you open it from Tools › Telemetry." / "Dashboard: status unavailable." / "Dashboard: not checked yet."; button "Stop the dashboard now"; toast "Dashboard stopped; it starts again when you open it." | renamed | The current stopped sentence says "from the lab header": false once #grafana-open moves to Tools (spec §1.3) — must change; test 10 asserts the exact string (G14). Keep the behaviour that Stop only rewrites #tele-grafana text (button disappears). |
| OPS-26 | #tele-auto checkbox "Automatic telemetry: configure the gNMI service…" + help (cEOS/XRv9k/cJunosEvolved, scoped commit, Prometheus, 15 minutes) | Telemetry settings dialog | Telemetry settings dialog | Label "Collect live interface statistics from this lab's devices (shown in the network dashboard)"; help "Works on Arista cEOS, Cisco XRv9k and Juniper cJunosEvolved devices once they finish starting. The manager adds only the few configuration lines needed for streaming and never overwrites your configuration. Data is kept for 15 minutes; stopping, destroying or redeploying the lab clears it." | renamed | Disabled-unless-usable stays; the summary sentence (OPS-23) is the reason text. |
| OPS-27 | #tele-profile "gNMI login" select (empty option = each node's saved login) + help | Telemetry settings dialog | Telemetry settings dialog › "Login used for telemetry" | Label "Login used for telemetry"; empty option "Same login as the CLI (saved credentials)"; help "Telemetry needs a username and password. Devices that log in with an SSH key need a password profile here. Passwords never leave the manager." | renamed | Test 10 asserts <option value="p" selected>Ops · arista_ceos</option> — keep option markup. |
| OPS-28 | Retry failed nodes (#tele-retry → POST telemetry/retry; rendered only with failures) | Telemetry settings dialog › actions | Telemetry settings dialog › actions | "Retry failed devices"; toast "Retrying telemetry on the failed devices." | renamed | id kept (test 10). |
| OPS-29 | Remove manager-added lines… (native confirm → POST remove-config; disabled while auto is on, title "Disable automatic telemetry first") | Telemetry settings dialog › actions | Telemetry settings dialog › actions (danger-outline) | "Remove telemetry configuration from devices…"; disabled title "Turn telemetry off first"; confirm "Remove the telemetry configuration lines this manager added on <lab>'s running devices? Your own configuration is not touched."; toasts "Removing telemetry configuration on {a, b}." / "Nothing to remove on the running devices." | renamed | Keep the native confirm() (test stubs it; it is a device-config change so a confirmation is justified even though spec §1.6 does not list it). |
| OPS-30 | Save (#tele-save → PUT telemetry/settings {auto, profile_id}; toasts; close; refresh) | Telemetry settings dialog › actions | Telemetry settings dialog › actions | "Save"; toasts "Telemetry turned on. Supported devices are set up as they become ready — open the network dashboard to watch them." / "Telemetry turned off; collection stopped." | renamed | Behaviour unchanged. |
| OPS-31 | Trailing help ("Remove deletes only the telemetry configuration lines this manager recorded as its own…") | Telemetry settings dialog | Telemetry settings dialog | "Remove deletes only the lines this manager added, on running devices. Turning telemetry off leaves the device configuration as it is." | renamed | — |
| OPS-32 | opReview mechanics: POST /operations/preview → token → confirm; name, path, backend warnings | Review dialog #operation-review | Same dialog for every entry point | Title template per action including the lab name (see G5); backend warnings stay as .op-notice | reorganized | Today the title is opLabels[action]+"?"; spec §1.6 wants "Destroy <lab>?" so a per-action title template is required. Preview errors still surface through the caller's opTask. |
| OPS-33 | Review copy: destroy | Review dialog | Review dialog | Title "Destroy <lab>?"; body per spec §1.6; confirm "Destroy lab" (danger) | renamed | Spec-covered. The backend cleanup warning ("Cleanup removes generated lab artifacts. Expected lab directory: …") moves under Technical details. |
| OPS-34 | Review copy: redeploy / redeploy with cleanup | Review dialog | Review dialog | "Redeploy <lab>?" per spec §1.6; confirm "Redeploy lab"; cleanup variant adds the folder sentence | renamed | Spec-covered. |
| OPS-35 | Review copy: stop / restart | Review dialog | Review dialog | "Stop devices?" per spec §1.6; "Restart devices?" (OPS-10); confirm "Stop devices" / "Restart devices" | renamed | Spec covers stop only. |
| OPS-36 | Review copy: deploy / start | Review dialog | Review dialog | deploy: "Start <lab>?" — "Creates and starts the {n} devices in this topology. Nothing is deleted; device logins open as the devices boot." [Start lab]; start: "Start <lab>'s stopped devices?" — "Stopped devices start again with their existing configuration." [Start devices] | renamed | Not in spec (G5). The helper protocol needs the token confirm, so keep the dialog but with no danger notices. |
| OPS-37 | Review copy: apply (+ YAML diff <details open>) | Review dialog | Review dialog | "Apply topology changes to <lab>?" — "The running lab is updated to match the topology file. Devices removed from the file are destroyed; connectivity may be interrupted."; diff section "Topology file changes" (open); [Apply changes] | renamed | G5. |
| OPS-38 | Review copy: save (containerlab save) | Review dialog | Review dialog | "Save device configurations on the VM?" — "Each supported device writes its running configuration to its startup configuration on the VM (containerlab save). This is separate from Save progress." [Save configurations] | renamed | G5. |
| OPS-39 | Review copy: inspect (this lab) | Review dialog | Review dialog | "Refresh the device list for <lab>" — "Reads the current state of this lab's devices from the VM. Nothing is changed." [Show devices] | renamed | Read-only: no notices, no "n devices affected" line (G5). |
| OPS-40 | Review copy: inspect-all ("View running lab details?") | Sidebar › View running lab details → Review dialog (shows "manager" + "0 deployed containers affected") | Manager ▾ › "Running labs on the VM…" (#inspect-all) → Review dialog | Title "Refresh the list of running labs on the VM"; confirm "Show running labs" (spec §0.6) | moved+renamed | Hide the <strong>manager</strong> name line and the affected count for inspect-all (screenshot 10-inspect-all shows both). |
| OPS-41 | Review copy: delete (YAML) notice "Deletes the original YAML on the VM after preserving a recovery copy…" | Review dialog | Review dialog | See OPS-21 | renamed | G5. |
| OPS-42 | Review copy: create (new topology file) | Review dialog | Review dialog | "Create this topology file on the VM?" — "Writes <path> on the VM. No devices are started until you deploy it." [Create file] | renamed | G5. |
| OPS-43 | Review copy: clone | Review dialog | Review dialog | "Download <project>?" — "Downloads <url> into the VM's lab folder. Existing folders are never overwritten." [Download] | renamed | G5. |
| OPS-44 | Review shared parts: "This can interrupt lab connectivity and open SSH sessions.", "{n} deployed containers affected", "Affected containers", "Containerlab command" (JSON-quoted argv), "Confirm to run this action on the VM…", "All deployed labs on the configured VM" | Review dialog | Review dialog | Notice "Open CLI sessions to this lab will disconnect." (stop/restart/redeploy/destroy/apply); "{n} running devices affected"; one <details> "Technical details" holding "Devices" (name · state) and "Command run on the VM" <pre>; help "Runs on the lab VM. If the lab changes before you confirm, this check is repeated."; "All labs on the VM" | renamed | Spec §1.6 puts the destroy details under "Technical details"; apply the same structure to every action. |
| OPS-45 | Confirm behaviour: POST /operations/confirm → close review, close #op-editor if open, refresh(), opShowJob(job.id) | Review dialog › confirm | Same | — | unchanged | Auto-opening the output modal after every confirm contradicts Journey 2 ("confirm → header shows Starting lab…") and the banner's [View output] (G7). #op-editor id must stay. |
| OPS-46 | Operation output dialog (#op-job-banner, <pre> output, 1 s poll while open, follow-scroll within 30 px) | #operation-output | Same dialog, opened from banner [View output], history rows, review confirm | Title "<operation label> · <lab>"; placeholder "Waiting for the VM…" | renamed | Keep the <pre> as the scroll container (max-height 48vh; overflow auto) or auto-follow breaks; keep op-banner good/bad/running and inspection-dialog classes (css.md §20). |
| OPS-47 | Banner strings (opJobBanner: "✔ {label} succeeded" / "✖ {label} failed / interrupted" / "{label} {status}…"; detail "{name} · Exit n · {message}") | Output dialog › #op-job-banner | Same | Same pattern with student opLabels values (G6), e.g. "✔ Start lab succeeded", "✖ Destroy lab failed"; detail "<lab> · <message>"; "Exit code n" moved to a Details line (always present, highlighted when non-zero) | renamed | Test 6 asserts the exact strings "✔ Deploy lab succeeded", "✖ Destroy deployment failed", "ceos-pair · Exit 0 · Operation completed" — update them with the copy; the behavioural claim (tone per status) is unchanged. |
| OPS-48 | Inspection table (Topology, Lab, Node, Kind / image, State / health, IPv4 / IPv6; caption "{n} deployed nodes"; "No deployed nodes found.") | Output dialog › #op-job-result (dialog widened by .inspection-dialog) | Same | Caption "{n} running devices"; columns Topology, Lab, Device, Type / image, State / health, IPv4 / IPv6; empty "No labs are running on the VM." | renamed | Test 1 asserts <th>State / health</th> — keep that header verbatim. |
| OPS-49 | "Recovery copy: <code>path</code>" after a delete job | Output dialog › #op-job-result | Same | "A recovery copy of the deleted file was kept at <code>path</code>." | renamed | — |
| OPS-50 | "Browse cloned lab topologies" (#op-open-clone → opBrowse(result.project_path)) | Output dialog › #op-job-result | Same | "Choose a topology from the downloaded lab" (primary) | renamed | — |
| OPS-51 | Completion: await refresh() when the job leaves queued/running; polling stops when the dialog closes (final refresh skipped) | Output dialog | Same | — | unchanged | With the banner-first flow (G7) the dialog is often never opened; the 4 s state poll still updates header/banner. Document this in the spec. |
| OPS-52 | Operation history dialog (GET /operations; all labs or filtered; rows "<name> · <label>" + "<status> · <date>"; "No lab operations yet.") | #operation-history via sidebar, Lab actions dialog and workspace.html | Manager ▾ › Operation history… (all labs); Lab actions ▾ › Lab › Operation history… (this lab); Advanced › Lab operations | Title "Operation history" / "Operation history · <lab>"; intro "Past lab operations and their output are kept on this manager."; rows "<lab> · <operation label>" + "<Completed / Failed / Interrupted / Running / Waiting> · <relative time>"; empty "No lab operations yet. Starting, stopping or redeploying a lab shows up here." | moved+renamed | Keep data-job rows → opShowJob. Copy not in spec (G8); relativeTime from status.js. |
| OPS-53 | Lab Topologies browser (POST /operations/browse; folder tree with lazy load, load-once and retry-on-reopen; 500 entries; "◇" file rows → opEdit) | #op-browser via sidebar Deploy New Lab (workspace.html), empty panel #deploy-empty, #import-top when no lab | Home › [Deploy a new lab] (#home-deploy); Home empty state (#deploy-empty); Manager ▾ › Deploy a new lab… (#vm-projects); Lab actions ▾ › Lab › Lab files…; Tools › Lab files card | Title "Deploy a new lab" (from Home/Manager) or "Lab files · <lab>" (from a lab); subtitle = current folder | moved+renamed | Three framings and a lab-scoped start folder are unspecified and opBrowse ignores its 2nd argument (G9). Keep the "◇ " file prefix and the "Close and reopen this folder to retry." text (test 9). |
| OPS-54 | Browser toolbar: Lab folders / Parent folder / New topology / Clone repository / Popular labs | #op-browser › .actions | Same | "All lab folders" / "Up one folder" / "Write a new topology…" / "Download a lab from GitHub…" / "Browse popular labs…" | renamed | ids op-roots/op-up/op-create/op-clone/op-popular kept (op-clone asserted). |
| OPS-55 | Clone/Popular start disabled and are enabled after GET capabilities (network + clone.available); help suffix for no network / failed check | #op-browser | Same | No network: "Downloading labs from the internet is turned off on this VM. An administrator can enable it (--allow-downloads) in VM setup."; failed: "Files are available, but the VM command check failed — open Diagnostics for details. Online downloads stay off." | renamed | Test 9 asserts /Files are available/ — keep that prefix; keep the isConnected guard. |
| OPS-56 | Browser help ("Expand a folder and select a .clab.yaml or .clab.yml…"; 500 entries) and empty text "No lab topologies or subfolders here." | #op-browser | Same | "Open a folder and pick a topology file (.clab.yaml) to view or deploy it. Only topology files are listed, up to 500 per folder."; empty "No topology files in this folder." | renamed | — |
| OPS-57 | Topology viewer/creator dialog (Absolute VM path, Topology YAML / File contents textarea, readonly rules, starter YAML, help) | #op-editor (from the browser) | Same | Title "Topology file" / "New topology file"; "File location on the VM"; "Topology (YAML)" / "File contents"; help "Deploy lab adds this lab to My labs and starts its devices on the VM. Add without starting keeps it in My labs only. Existing files can't be edited here — edit them on the VM." | renamed | Readonly behaviour and the starter YAML unchanged (advanced authoring). |
| OPS-58 | Validate / preview topology (#op-validate → opParse → opMapPreview) | #op-editor › actions | Same | "Preview topology" | renamed | yaml-only condition unchanged. |
| OPS-59 | Review creation on VM (#op-save-yaml → opReview create) | #op-editor › actions (new files) | Same | "Create file on the VM…" | renamed | Review copy OPS-42. |
| OPS-60 | Save to manager (#op-add-project: re-read VM file → opParse → op-add-confirm → POST /lab-definitions → PUT operations-settings → activeId + sessionStorage → toast) and the unreachable "Link topology" branch | #op-editor › actions + #op-add-confirm | Same | "Add to My labs without starting"; confirm title "Add <name> to My labs?" body "<name> · {n} devices[ · layout from the saved diagram file]. Adds the lab to My labs and remembers where its topology file is on the VM. No devices are started. Device logins can be imported after the lab is deployed." [Add lab]; toast "<name> added to My labs." | renamed | Behaviour unchanged (re-reads the VM file, ignores editor text). The labId branch stays in code but unreachable, as today — do not delete before parity is proven; note it in docs. |
| OPS-61 | Deploy lab (#op-deploy-project: opSaveWorkspace reuse-by-deployment_name-or-path else create → opReview deploy; editor closed on confirm) | #op-editor › actions (primary) | Same | "Deploy lab" (primary) → review "Start <name>?" | renamed | Tests 7/12 assert reuse, sessionStorage.activeLab and the annotations part — unchanged. In the new shell the confirm must also navigate to the lab (selectLab(id), rememberOpened) instead of relying on auto-select, which spec §1.2 removes. |
| OPS-62 | Clone dialog (HTTPS repository URL, New project folder name, --allow-downloads text, "Review clone") | #op-clone-dialog | Browser › "Download a lab from GitHub…" and from Popular labs | Title "Download a lab from GitHub"; "Repository address (https://github.com/owner/repository)"; "Folder name on the VM"; text "The lab is downloaded into the VM's lab folder (/srv/containerlab-node-manager/projects). Existing folders are never overwritten. Afterwards pick a topology from it to deploy. Needs internet downloads enabled on the VM."; [Download…] | renamed | Review copy OPS-43; finished job offers OPS-50. |
| OPS-63 | Popular labs dialog (GET /operations/popular → opClone(url,name)) | #op-popular-dialog | Browser › "Browse popular labs…" | Title "Popular labs"; text "Community containerlab labs on GitHub (tagged clab-topo, most-starred first). Downloading and deploying are separate steps you confirm." | renamed | — |
| OPS-64 | Topology preview dialog (svg from topologyMarkup/measureTopology; positioned vs default-grid sentence) | #op-map-preview | Same | Title "Topology preview · <name>"; "Wiring from the topology file; device positions from its saved diagram file." / "Wiring from the topology file. Devices sit on a default grid — arrange them later with Edit diagram." | renamed | — |
| OPS-65 | Edit diagram (#map-edit → opLayout → editDiagram) | Topology › map tools | Topology › map tools #map-edit (spec §1.3) + Lab actions ▾ › Lab › Edit diagram | Edit diagram | unchanged | #map-edit is dereferenced without a null guard in the wiring block — it must exist at load. |
| OPS-66 | Quick Start/Deploy (#lab-start): opQuickActions gating (path present, discovery.connected, known status, not busy, status != Running); titles "Deploy this topology…" / "Start stopped devices…" | Deployment bar › "Start lab" (primary) | Lab actions ▾ › Lifecycle › #lab-start; Home lab card [Start lab] (navigates then clicks #lab-start); situational banner [Start lab] | "Start lab" (startAction deploy) / "Start stopped devices" (startAction start) + a visible reason when disabled | moved+renamed | Test 5 (opQuickActions) unchanged. Spec's label rule is inverted (G2); the Home proxy is a silent no-op when gating fails because opQuickRun returns without a message and #lab-start.disabled is only computed for current() (G3). |
| OPS-67 | Quick Destroy (#lab-destroy: refresh capabilities, opDestroyOptions, opReview destroy) | Deployment bar › "Destroy lab" (danger-outline, beside Start) | Lab actions ▾ › Danger › #lab-destroy | Destroy lab… | moved+renamed | Gating unchanged (canDestroy). Add a disabled reason: "Nothing to destroy — the lab is not deployed" / "Connect the VM first" / "Wait for the current operation to finish" (G2). |
| OPS-68 | #operation-summary "Lab command running · view operation history" (sidebar role=status) | Sidebar | Lab workspace › situational banner #lab-banner (spec §1.3: "Starting lab… [View output]", #operation-summary text moves here) | "<Starting lab / Stopping lab / Redeploying lab / Restarting devices / Applying topology / Destroying lab>… [View output]" | moved+renamed | The banner needs the running job id for [View output] (state.operations.find(j => j.lab_id===lab.id && queued/running)) — unspecified (G7). Keep the #operation-summary id inside the banner. |
| OPS-69 | Busy-state disabling of #remove-lab, #sync-vm, #update-definition, #link-deployment in renderLabOperations (re-enables only the last two when idle) | Deployment bar buttons | Same ids inside Lab actions ▾ (sync-vm, remove-lab) and Advanced › Deployment details (update-definition, link-deployment) | — | unchanged | render() must keep calling renderManagement() before renderLabOperations() or the busy disable no longer wins. |
| OPS-70 | Sidebar "Deploy New Lab" (#vm-projects → location.assign("/static/workspace.html#mode=folder")) | Sidebar (side-primary) | Manager ▾ › "Deploy a new lab…" (#vm-projects) | Deploy a new lab… | moved+renamed | Spec is silent on whether the item still leaves the app for the two-card page the audit flags (§5) — G10. |
| OPS-71 | Empty-panel "Deploy a new lab" (#deploy-empty → openDeploy; disabled with title "Connect the VM to browse its topologies" when disconnected) | Empty panel | Home › empty state (#deploy-empty) | Deploy a new lab | unchanged | Gating stays in management.js renderLanding; #home-deploy and the Manager ▾ item need the same gating and reason (G9). |
| OPS-72 | Sidebar "Operation history" (#operations-history → opHistory() for all labs) | Sidebar | Manager ▾ › Operation history… | Operation history… | moved | — |
| OPS-73 | Sidebar "View running lab details" (#inspect-all → opReview inspect-all) | Sidebar | Manager ▾ › Running labs on the VM… | Running labs on the VM… | moved+renamed | Copy per spec §0.6 (OPS-40). |
| OPS-74 | Right-click / ContextMenu key / Shift+F10 on a lab in the sidebar ([data-lab] inside #labs) → openLabOperations(lab) | Sidebar › lab list | UNMAPPED — spec moves nav#labs into details#lab-switcher (the handler would still fire there) but the Home lab cards, the new primary lab list, get no equivalent | — | UNMAPPED | G12. |
| OPS-75 | opNewTab popup-blocked fallback dialog "Open workspace" ("Your browser may have blocked the new tab." + "Open workspace ↗" link with rel=opener) | #op-open-tab | Same (only reached from Open all CLIs ↗ once G10 is adopted) | Title "Open CLI sessions"; "Your browser blocked the new tab. Use this button instead:" [Open CLI sessions ↗] | renamed | Keep target=_blank rel=opener (the launcher needs window.opener). |
| OPS-76 | Standalone /static/workspace.html: "Deploy New Lab" landing (Lab Topologies / Operation history cards, "← Back to lab manager", "Debug panel · Diagnose VM helper…") and SSH-launcher mode ("SSH sessions · <lab>", "Open all ready sessions ↗", per-node "SSH ↗", "Unavailable or missing credentials", 32-popup cap) | /static/workspace.html (#mode=folder, #mode=ssh; loads operations.js) | UNMAPPED — spec §1.1 only says "other pages keep their own headers"; no role or copy for this page | — | UNMAPPED | G11. Test 8 asserts #workspace-title === "Deploy New Lab" and that the browser does not auto-open. |
| OPS-77 | #sync-vm "Sync from VM" (management.js: shown when lab.deployment_name; disabled when syncing, disconnected or !vm_source.can_sync; label "Syncing…") | Deployment bar | Lab actions ▾ › Lab › "Sync topology from VM" — spec: "hidden unless updates available, as today" | Sync topology from VM (updates available) | behaviour-changed | Spec's rule is not today's rule and removes the manual re-sync when nothing is flagged (G15). Cross-file (management.js) but placed by this module's menu. |
| OPS-78 | opCaps shared capability cache (reset to null by management.js after Remove lab); on error everything stays enabled and destroy falls back to cleanup:true | Internal | Unchanged | — | unchanged | The new menu gating (G4) must read the same cache and inherit the same fail-open rule. |
| OPS-79 | sessionStorage.activeLab + activeId switch after Save-to-manager / Deploy | Internal | Unchanged; the new shell must additionally call selectLab(id)/rememberOpened(id) since refresh() no longer auto-selects | — | unchanged | Test 7 asserts the key name. |
| OPS-80 | Main-page wiring guard if($("import-top")) with unguarded $() dereferences (#map-edit, #lab-actions, #vm-projects, #lab-start, #lab-destroy, #operations-history, #inspect-all, #labs) and the #lab-actions injection | operations.js bottom block | All ids exist in the new shell per spec (Advanced › Lab source, map tools, Lab actions ▾, Manager ▾, lab switcher) | — | reorganized | Every id must be present in index.html at load even when hidden (Home first); duplicate-id risk for #lab-actions (G1). |
| OPS-81 | opInspectionRows tolerant parser (grouped/flat JSON inside log noise; 10,000 row cap; escaping) | Internal | Unchanged | — | unchanged | Tests 1–3. |
| OPS-82 | Unused globals opName, opMenuLab, opEditorContext | Internal | Keep | — | unchanged | Brief: no deletion until parity is proven; harmless. |

## Spec gaps (UNMAPPED / behaviour-changed / missing copy)

### G1 — OPS-03, OPS-80 (G1)

**Problem.** operations.js injects <button id="lab-actions"> before #import-top unconditionally and renderLabOperations sets its hidden flag. Spec §1.3 puts #lab-actions inside details#lab-actions-menu as "All lab operations…" and §1.3/§1.2 put #import-top in Advanced › Lab source, but never says who creates the element. If index.html declares it in the menu, the injection produces a duplicate id and $("lab-actions") binds the first match (the wrong element); if index.html does not declare it, the button lands next to "Replace inventory…" in Advanced, not in the menu. Also, the whole wiring block is skipped when #import-top is absent at load (e.g. a Home-only render).

**Proposed fix.** Add to §1.3: "index.html declares <button id='lab-actions' class='menu-item'>All lab operations…</button> inside details#lab-actions-menu (Lab group). operations.js injects its fallback button only if(!$('lab-actions')). renderLabOperations keeps #lab-actions.hidden=!current(). #import-top, #map-edit, #vm-projects, #lab-start, #lab-destroy, #operations-history, #inspect-all and nav#labs must all be present in index.html at page load, hidden on Home, because operations.js binds them once inside if($('import-top'))."

### G2 — OPS-66, OPS-67, OPS-08 (G2)

**Problem.** Spec §1.3: "#lab-start … when the lab is running the item reads Start stopped devices". opQuickActions makes canStart=false when status==="Running", so a Running lab shows a disabled item with a label that promises an action. The relabel only fits "Stopped" and "Partially running" (startAction==="start"). Neither #lab-start nor #lab-destroy explains why it is disabled (inventory §4: only two title strings), contradicting the brief ("Do not simply leave the button disabled with no explanation").

**Proposed fix.** Label by startAction: deploy → "Start lab", start → "Start stopped devices". When disabled, set title and render a .menu-hint line with the first matching reason: !discovery.connected → "Connect the VM first"; busy() → "Wait for the current operation to finish"; !opPath(lab) → "This lab has no topology file on the VM (Advanced › Lab source)"; status Running → "The lab is already running"; unknown status → "Lab status is unknown — refresh the lab list". For #lab-destroy: status "Not deployed" → "Nothing to destroy — the lab is not deployed". Add these to §1.3 and to opQuickActions' return value ({reason}) so test 5 can assert them.

### G3 — OPS-66 (G3)

**Problem.** Spec §1.2: the Home card [Start lab] "navigates to the lab and clicks #lab-start". #lab-start.disabled is only computed by renderLabOperations() for current(), and opQuickRun returns silently when canStart is false. Clicking on Home before the lab page has rendered, or when the VM is disconnected/busy/no path, does nothing and says nothing.

**Proposed fix.** Home card computes opQuickActions(lab, state.discovery, busy()) itself, renders [Start lab] disabled with the G2 reason text when !canStart, and on click runs selectLab(id); render(); $("lab-start").click() in that order. State this in §1.2.

### G4 — OPS-06, OPS-09, OPS-10, OPS-14, OPS-15 (G4)

**Problem.** The new Lab actions ▾ items [data-op-action=stop|restart|redeploy] and "[data-op-action=redeploy-cleanup]" have no gating in the spec. Today the dialog gates each command by opCaps.actions[a].available (fetched lazily inside openLabOperations) and the quick actions by opQuickActions (connected, known status, path, not busy). A bare menu item wired to opReview can be clicked while the VM is disconnected or a job is running and fails with a raw toast. "redeploy-cleanup" is not a backend action: POST /operations/preview accepts action "redeploy" with options {cleanup:true}, so the spec's selector would 4xx.

**Proposed fix.** Spec §1.3 gating rule: on details#lab-actions-menu toggle (open), call opCapabilities() (cached opCaps, fail-open as today) and disable items whose capability reports available:false with the hint "Not available on this VM — see Diagnostics". All lifecycle items require opQuickActions().available; stop/restart additionally require status in {Running, Partially running}; redeploy requires status != "Not deployed"; the cleanup item is data-op-action="redeploy" data-op-options="{&quot;cleanup&quot;:true}" data-op-variant="cleanup" and is rendered only when opCaps.actions.redeploy.cleanup. Failures from menu items render in #lab-banner (danger) with the backend message under Details instead of the toast.

### G5 — OPS-32 … OPS-44 (G5)

**Problem.** Spec §1.6 rewrites the review dialog for destroy, redeploy, stop and (implicitly) restart only. opReview builds one title (opLabels[action]+"?") and one generic body for every action; deploy, start, apply, save, inspect, delete, create and clone keep "Confirm to run this action on the VM", "{n} deployed containers affected", "Affected containers", "Containerlab command" and a "<strong>manager</strong>" name line (inspect-all).

**Proposed fix.** Add a per-action table to §1.6 (title / body / confirm): deploy "Start <lab>?" / "Creates and starts the {n} devices in this topology. Nothing is deleted; device logins open as the devices boot." / "Start lab"; start "Start <lab>'s stopped devices?" / "Stopped devices start again with their existing configuration." / "Start devices"; restart "Restart devices?" / "Devices restart from their startup configuration; unsaved running-config changes may be lost on some platforms. Open CLI sessions disconnect." / "Restart devices"; apply "Apply topology changes to <lab>?" / "The running lab is updated to match the topology file. Devices removed from the file are destroyed; connectivity may be interrupted." + "Topology file changes" diff / "Apply changes"; save "Save device configurations on the VM?" / "Each supported device writes its running configuration to its startup configuration on the VM (containerlab save). This is separate from Save progress." / "Save configurations"; inspect "Refresh the device list for <lab>" / "Reads the current state of this lab's devices from the VM. Nothing is changed." / "Show devices"; inspect-all per §0.6 with the name and affected lines hidden; delete "Delete <lab>'s topology file from the VM?" / "<path> is deleted after a recovery copy is kept. The lab stays in My labs and your saved progress is untouched." / "Delete file"; create "Create this topology file on the VM?" / "Writes <path> on the VM. No devices are started until you deploy it." / "Create file"; clone "Download <project>?" / "Downloads <url> into the VM's lab folder. Existing folders are never overwritten." / "Download". Shared: notice "Open CLI sessions to this lab will disconnect." for stop/restart/redeploy/destroy/apply; "{n} running devices affected"; a single <details> "Technical details" (Devices list + "Command run on the VM" <pre>); help "Runs on the lab VM. If the lab changes before you confirm, this check is repeated."; "All labs on the VM" instead of "All deployed labs on the configured VM". Mechanics (preview → token → confirm) unchanged.

### G6 — OPS-47, OPS-52 (G6)

**Problem.** opLabels feeds the review title, the output banner ("✖ Destroy deployment failed") and history rows; test 6 asserts those exact strings and test 6/history use "Destroy deployment", "Inspect lab". Spec §2 introduces operationLabel(action) in status.js but never says whether opLabels is replaced, so implementers may leave "Destroy deployment"/"Save configurations (clab)" in the banner and history while the menu says "Destroy lab".

**Proposed fix.** State in §2/§4: opLabels values become the student labels (deploy "Start lab", redeploy "Redeploy lab", destroy "Destroy lab", apply "Apply topology changes", start "Start devices", stop "Stop devices", restart "Restart devices", save "Save device configurations", inspect "Show running devices", inspect-all "Running labs on the VM", create "Create topology file", delete "Delete topology file", clone "Download lab"); operationLabel(action) in status.js returns opLabels[action] (single source). opJobBanner keeps its {tone,title,detail} shape and ✔/✖ prefixes; detail becomes "<name> · <message>" with "Exit code n" rendered as a separate Details line. tests/test_operations_ui.js test 6 strings are updated accordingly (behavioural claim unchanged).

### G7 — OPS-45, OPS-51, OPS-68 (G7)

**Problem.** After confirm, opReview always opens the operation-output modal (opShowJob). Spec Journey 2 and §1.3 describe a banner-driven flow ("Starting lab… [View output]") but do not say whether the modal still pops up; if it does, the calm journey is broken by a wall of containerlab output. The banner's [View output] also needs a job id, which the spec does not source. Polling and the final refresh() only run while the dialog is open.

**Proposed fix.** Add to §1.3/§1.6: for lifecycle actions on the current lab (deploy, start, stop, restart, redeploy, destroy, apply) confirm closes the review, shows the toast "<label>…" and relies on #lab-banner (text from #operation-summary: "<label>… [View output]") where [View output] calls opShowJob(job.id) with job = state.operations.find(j => j.lab_id===lab.id && ["queued","running"].includes(j.status)); for result-bearing actions (inspect, inspect-all, save, create, delete, clone) opShowJob opens immediately as today. Note that the 4 s state poll keeps header/banner current when the output dialog is closed.

### G8 — OPS-52 (G8)

**Problem.** The history dialog copy ("Saved command output remains in persistent manager storage.", raw status words, toLocaleString timestamps, "No lab operations yet.") is not addressed by the spec although the dialog is reachable from three places in the new IA.

**Proposed fix.** Title "Operation history" (Manager ▾) / "Operation history · <lab>" (lab menu, opHistory(lab.id)); intro "Past lab operations and their output are kept on this manager."; row "<lab> · <operationLabel>" + "<Completed|Failed|Interrupted|Running|Waiting> · <relativeTime>"; empty "No lab operations yet. Starting, stopping or redeploying a lab shows up here." Keep data-job rows → opShowJob.

### G9 — OPS-53, OPS-71 (G9)

**Problem.** The same opBrowse dialog is reached as "Deploy a new lab" (Home #home-deploy, Home empty state #deploy-empty, Manager ▾ #vm-projects) and as "Lab files…" (Lab actions ▾, Tools card). Spec says "opens the existing topology/file browser for this lab" without saying which folder; opBrowse(path) ignores its 2nd argument (workspace.js already passes activeId), so there is no lab scoping. #home-deploy and the Manager item also lack the discovery.connected gating that #deploy-empty has (management.js disables it with "Connect the VM to browse its topologies").

**Proposed fix.** Define opBrowse(path="", labId="") in §1.3/§4: labId sets the title "Lab files · <lab>" and the start folder dirname(opPath(lab)); without it the title is "Deploy a new lab". The tree keeps calling opEdit(entry.path) without labId (today's behaviour: Deploy reuses/creates a lab by deployment name; the "Link topology" branch stays unreachable and is documented as such). #home-deploy and #vm-projects are disabled with the reason "Connect the VM to browse its lab topologies" whenever !state.discovery.connected, mirroring #deploy-empty.

### G10 — OPS-70 (G10)

**Problem.** #vm-projects today navigates the tab to /static/workspace.html#mode=folder, a separate two-card landing ("Lab Topologies" / "Operation history", "← Back to lab manager") that the UX audit §5 flags. Spec §1.1 relabels it "Deploy a new lab…" under Manager ▾ but does not say whether it still leaves the app; the Home button #home-deploy opens the in-app dialog, so the two "Deploy a new lab" entries would behave differently.

**Proposed fix.** Spec §1.1: #vm-projects calls openDeploy() (the in-app browser dialog), identical to #home-deploy. /static/workspace.html stays for the SSH launcher (opNewTab({mode:"ssh"})) and deep links (#mode=folder keeps working, unlinked). Test 8 (title "Deploy New Lab", no auto-open) remains valid.

### G11 — OPS-76 (G11)

**Problem.** The standalone /static/workspace.html (loads operations.js) is unmapped: its "Deploy New Lab" landing and its SSH-launcher mode ("SSH sessions · <lab>", "Open all ready sessions ↗", "SSH ↗", "Unavailable or missing credentials", "← Back to lab manager", "Debug panel · Diagnose VM helper and file-browsing errors") keep admin wording and the old visual system.

**Proposed fix.** Add a §1.7 "Standalone pages": workspace.html is restyled with the §3 tokens; title stays "Deploy New Lab" (test 8) with subtitle "Choose a lab topology on your VM to launch"; "← Back to My labs"; "Diagnostics · If browsing files fails, check the VM connection here"; SSH mode: title "CLI sessions · <lab>", [Open all ready CLIs ↗], per-device [Open CLI ↗], not-ready text "Not ready or no login saved", and the 32-popup cap message. Its Operation history card calls opHistory() with the G8 copy.

### G12 — OPS-74 (G12)

**Problem.** Right-click / ContextMenu / Shift+F10 on a sidebar lab opens that lab's operations dialog (operations.js binds contextmenu and keydown on #labs). Spec moves nav#labs into details#lab-switcher and makes Home lab cards the primary lab list, but never mentions the shortcut, so the cards have no route to "All lab operations" for a lab that is not open.

**Proposed fix.** Generalise the handler to [data-lab] inside #labs, #home and #lab-switcher (same preventDefault + openLabOperations(id)). Add a subtle per-card button.icon-button aria-label="More actions for <lab>" ("⋯") on Home that calls openLabOperations(lab.id); the dialog is titled with the lab name so the target is unambiguous. Keyboard: the card button is focusable, so Shift+F10 on it works via the delegated keydown.

### G13 — OPS-17 (G13)

**Problem.** Spec §1.2 sorts Home cards "favourites first" but offers no way to set a favourite; the only toggle is Lab actions ▾ › All lab operations… › "Favorite lab", three clicks deep and named in admin terms.

**Proposed fix.** Home lab card gets a star toggle (button.icon-button, aria-pressed, aria-label "Add to favourites" / "Remove from favourites") calling the existing PUT /labs/{id}/operations-settings {favorite:!lab.favorite} then refresh(); the dialog button is relabelled "Add to favourites" / "Remove from favourites". Cards render a filled star for favourites (not colour-only).

### G14 — OPS-23 … OPS-31 (G14)

**Problem.** The telemetry settings dialog keeps every engineering term (gNMI service, scoped commit, Prometheus scrape, password profile, "Grafana: running on TCP 3000", "Remove manager-added lines…") and the spec gives it no copy. One sentence becomes wrong: "Grafana: stopped; it starts when you open it from the lab header." — spec §1.3 moves #grafana-open to Tools › Telemetry. Test 10 asserts most of these strings verbatim.

**Proposed fix.** Adopt the copy in rows OPS-23…31 (summary sentences, "Devices that need attention", "Dashboard: …" lines with "from Tools › Telemetry", checkbox "Collect live interface statistics from this lab's devices (shown in the network dashboard)", select "Login used for telemetry" with "Same login as the CLI (saved credentials)", buttons "Retry failed devices" / "Remove telemetry configuration from devices…" (disabled title "Turn telemetry off first") / "Save" / "Stop the dashboard now"). Keep ids, option markup, <strong>name</strong> (state) rows and the native confirm(); update the test 10 regexes to the new sentences while keeping every behavioural assertion (call order, payloads, disabled states).

### G15 — OPS-77 (G15)

**Problem.** Spec §1.3: "#sync-vm Sync topology from VM (hidden unless updates available, as today)". Today management.js shows it whenever lab.deployment_name exists and disables it when syncing, disconnected or !vm_source.can_sync; "VM files: Updates available" is a separate status line. Hiding the item until the manager flags updates removes the manual re-sync and misdescribes current behaviour.

**Proposed fix.** Keep today's visibility/disabled rules; label "Sync topology from VM", with the suffix " (updates available)" when the VM-files status reports updates; when disabled show the reason as a .menu-hint ("Connect the VM first" / "Sync already running" / "This lab's files cannot be synced — see Advanced › Deployment details"). Correct the "as today" claim in §1.3.

### G16 — OPS-15 (G16)

**Problem.** opCommand renders "<small>Unavailable on this VM</small>" with no reason; the capabilities API exposes only available:true/false. The spec does not rewrite it.

**Proposed fix.** Copy: "Not available on this VM — see Diagnostics" (Diagnostics = /static/debug.html, linked from Manager ▾). Keep the disabled attribute and the data-op-* attributes.

### G17 — OPS-01 (G17)

**Problem.** Every operations dialog carries the eyebrow "NODE MANAGER" (product framing, not the task). Spec §3 keeps .eyebrow for dialog heads but gives no text and opDialog(id,title,body) is a cross-file contract.

**Proposed fix.** opDialog(id,title,body,eyebrow="Lab") — optional 4th argument, backward compatible. Values: review "Confirm", output "Lab operation", history "History", browser "Deploy" (or "Lab files"), editor "Topology file", clone/popular "Download", telemetry "Telemetry", operations dialog "All lab operations", open-tab fallback "Open CLI".

### G18 — OPS-04 … OPS-22 (G18)

**Problem.** The two-column "Lab actions" dialog (retained by the spec as "All lab operations…") keeps its admin layout: destroy and delete sit among routine buttons, "Favorite lab" beside "Delete undeployed VM YAML", help text quoting containerlab flags. Spec §4 says only "dialog copy".

**Proposed fix.** Restructure the dialog body (same id, same data-op-action buttons): section "Deployment" (Deploy lab, Redeploy lab, Start stopped devices, Stop devices, Restart devices, Apply topology changes, Show running devices, Save device configurations on the VM, plus the "…and clear the lab folder" variants when capabilities allow), section "Lab tools" (Open all CLIs ↗, Edit diagram, Telemetry settings…, Operation history…, Add/Remove favourites), section "Danger" (.menu-danger styling: Destroy lab…, Delete the topology file from the VM…). Path line and help per OPS-04/OPS-22. Eyebrow "All lab operations", title = lab name.

### G19 — OPS-53 … OPS-64 (G19)

**Problem.** The deploy-a-new-lab flow (browser → topology file viewer → Add to My labs / Deploy → review → output → "Browse cloned lab topologies") is the Journey 2 entry for a fresh lab, yet none of its copy is in the spec: "Lab Topologies", "Absolute VM path", "Review creation on VM", "Save to manager", "Link topology", "HTTPS repository URL", "SRL Labs repositories tagged clab-topo", "annotations file", "--allow-downloads".

**Proposed fix.** Adopt the copy in rows OPS-53…64 verbatim (titles "Deploy a new lab" / "Lab files · <lab>", toolbar "All lab folders / Up one folder / Write a new topology… / Download a lab from GitHub… / Browse popular labs…", editor "Topology file" / "File location on the VM" / "Preview topology" / "Create file on the VM…" / "Add to My labs without starting" / "Deploy lab", add-confirm "Add <name> to My labs?", clone "Download a lab from GitHub", popular "Popular labs", preview sentences). Keep the "◇ " prefix, "/Files are available/" and "Close and reopen this folder to retry." strings that test 9 asserts.

### G20 — OPS-00 (spec meta)

**Problem.** Spec §0.1 and the header require "every row of inventory/MERGED-INVENTORY.md" to remain reachable, but that file does not exist in the scratchpad inventory folder (only per-module files: operations.md, management.md, git-progress.md, …). The regression checklist the non-negotiable points at is undefined.

**Proposed fix.** Either generate inventory/MERGED-INVENTORY.md from the per-module files or amend §0.1 to point at the per-module inventories plus the parity tables under parity/ (this file for operations). The final report's "Functional Parity" section should be built from the parity tables.

