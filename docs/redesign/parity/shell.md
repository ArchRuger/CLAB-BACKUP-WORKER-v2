# Parity review — shell module (index.html + app.js) against DESIGN-SPEC.md

Sources: `inventory/shell.md` (105 capabilities, 5 dialogs), `DESIGN-SPEC.md` §0–§5, `UX-AUDIT.md`, screenshots `shots/before/01…23`, and the load-order facts re-verified in `app.js`, `management.js`, `operations.js`, `git-progress.js`, `capture.js` (read-only).

Change vocabulary: unchanged · moved · renamed · moved+renamed · reorganized · behaviour-changed · UNMAPPED. "Spec location" is written as screen › tab › section › control. Gap ids (G1…G17) refer to §4 below.

## 1. Capability mapping

| # | id | Current label | Current location | Spec location | New label | Change | Note |
|---|---|---|---|---|---|---|---|
| 1 | sidebar.brand | Containerlab Node Manager / NETWORK LAB OPERATIONS | Sidebar › top | Top bar › left › brand mark + `#nav-home` (link to Home) | Containerlab Node Manager | moved+renamed | Drop the "NETWORK LAB OPERATIONS" tagline; brand becomes the Home link. |
| 2 | sidebar.lab-list | lab buttons `#labs` | Sidebar › LAB WORKSPACES | Top bar › breadcrumb › lab switcher `details#lab-switcher` (body = `nav#labs`); Home › Lab cards `#lab-cards` | Switch lab (summary aria-label) · lab cards | reorganized | render() keeps writing `#labs`; sub-text must use labState, not raw `deployment.status` (G15). |
| 3 | sidebar.select-lab | click a lab | Sidebar › `#labs` `[data-lab]` | Top bar › lab switcher › lab button; Home › lab card › [Open lab] | Open lab | reorganized | selectLab must also close the switcher, write localStorage `clab.lastLab`/`clab.opened.<id>` and the `#lab=` hash; still clears `#search`, `#log-job`, sets tab topology. |
| 4 | sidebar.lab-context-menu | right-click / Shift+F10 on a lab | Sidebar › `#labs` | Top bar › lab switcher › lab button (right-click / Shift+F10) | Lab actions (context menu) | moved | Delegation on `#labs` survives; Home cards get no context menu (G14). |
| 5 | sidebar.deploy-new-lab | Deploy New Lab `#vm-projects` | Sidebar › primary button | Top bar › Manager ▾ › Deploy a new lab… | Deploy a new lab… | moved+renamed | Still navigates to `/static/workspace.html#mode=folder`; Home's `#home-deploy` uses a different flow (G12). |
| 6 | sidebar.view-running-lab-details | View running lab details `#inspect-all` | Sidebar | Top bar › Manager ▾ › Running labs on the VM… | Running labs on the VM… | moved+renamed | Keeps the token/confirm flow; dialog copy "Refresh the list of running labs on the VM" / confirm "Show running labs" (spec §0.6). |
| 7 | sidebar.vm-connection | VM connection `#vm-settings` | Sidebar | Top bar › Manager ▾ › VM connection… | VM connection… | moved+renamed | Also the [VM connection] action of the "VM unreachable" banner. |
| 8 | sidebar.refresh-discovery | Refresh discovery `#vm-refresh` | Sidebar | Top bar › Manager ▾ › Refresh lab list | Refresh lab list | moved+renamed | management.js:146 restores the literal 'Refresh discovery' after the click (G5). Busy text "Checking the VM…". |
| 9 | sidebar.operation-history | Operation history `#operations-history` | Sidebar | Top bar › Manager ▾ › Operation history…; Lab actions ▾ › Lab › Operation history…; Advanced › Lab operations › [Operation history…] | Operation history… | moved+renamed | One id; the two lab-page entries are separate buttons calling `opHistory()`. |
| 10 | sidebar.manual-discovery | Manual discovery `#new-lab` | Sidebar | Top bar › Manager ▾ › Import lab files… | Import lab files… | moved+renamed | Effective handler is management.js `openSetup()`; delete the dead app.js `openImport` binding for this id or keep load order. |
| 11 | sidebar.operation-summary | status text `#operation-summary` | Sidebar (role=status) | Lab workspace › Situational banner `#lab-banner` (operation running: "Starting lab… [View output]") | (sentence) | moved | Lab-less operations (deploy from browser, inspect-all) have no slot on Home (G13). |
| 12 | sidebar.vm-summary | VM connected · checks every 30s `#vm-summary` | Sidebar (role=status) | Top bar › Manager ▾ › footer line | VM: connected / VM: not configured / VM: cannot be reached | moved | Keep role=status. "helper update required…" sentence stays (admin task). |
| 13 | sidebar.discovered-labs | discovered lab buttons `#discovered-labs` | Sidebar | Home › Other labs on the VM `#home-discovered` › list | Also running on the VM · [Import] | moved+renamed | Sub-text "Try importing VM files" → "Import"; "Ready to import · confirmation required" → "Ready to import". |
| 14 | sidebar.excluded-labs | excluded list `#excluded-labs` (right-click → clear exclusion) | Sidebar | Home › Other labs on the VM › Details (collapsed) | Not imported automatically | moved | contextmenu/keydown delegation on the id survives; hint "Import again · Right-click to clear exclusion" → add a visible [Import again] / [Allow again] pair (management module). |
| 15 | sidebar.discovery-file-details | Discovery file details `details#discovery-files` | Sidebar bottom | Home › Other labs on the VM › Details › Lab files found on the VM | Lab files found on the VM | moved+renamed | Empty text "No file results yet. Refresh discovery…" → "Nothing found yet. Use Manager › Refresh lab list." |
| 16 | sidebar.supported-release | Supported device types as of release 1.28.0 · Junos / IOS-XR / EOS | Sidebar bottom | Top bar › Manager ▾ › footer line | Works with Junos, IOS-XR and Arista EOS · v1.29.0 | moved | render() rewrites the text each poll — new wording must come from render() (G2). |
| 17 | sidebar.debug-panel-link | Debug panel (link) | Sidebar bottom | Top bar › Manager ▾ › Diagnostics | Diagnostics | moved+renamed | Same href `/static/debug.html`. |
| 18 | sidebar.manager-settings | Manager settings `#manager-settings` | Sidebar bottom | Top bar › Manager ▾ › Manager settings… | Manager settings… | moved+renamed | "Start fresh" confirmation kept. |
| 19 | shell.breadcrumb | Workspace / {lab or Overview} `#breadcrumb` | Top bar | Top bar › centre › My labs (`#crumb-home`) / `#breadcrumb` lab name | My labs / <lab> | renamed | render() writes 'Overview' when no lab; must hide `#breadcrumb` on Home instead. |
| 20 | shell.worker-state | Worker idle / SSH job in progress / Saving lab progress `#worker-state` | Top bar right | Top bar › right › activity indicator `#worker-state` | Saving progress… / Backing up… / Checking logins… / Restoring… / Lab operation running… (hidden when idle) | behaviour-changed | Needs job-kind detection (`state.jobs[].operation`, `state.operations`, restore jobs) — restore field unspecified (G13). Toggle `hidden`, never print "Worker idle". |
| 21 | shell.title | {lab name} or "Your next lab starts here." `h1#title` | Page heading | Lab workspace › Lab header › `h1#title`; Home › static h1 "My labs" | <lab name> · My labs | renamed | `#title` lives inside `#lab-content`; the fallback sentence is dead once Home has its own heading. |
| 22 | shell.subtitle | "Your nodes, connections, and configuration history." `#subtitle` | Page heading | UNMAPPED | — | UNMAPPED | Spec header has `p.lab-status-line` (`#lab-state`, `#lab-ready`, `#lab-progress`) and no `#subtitle`; app.js:48 writes it unguarded (G2). |
| 23 | shell.import-top | ↑ Replace inventory / Deploy a new lab `#import-top` | Page heading (red primary) | Lab workspace › Advanced › Lab source & deployment › [Replace device list…] | Replace device list (Ansible inventory)… | moved+renamed | On Home it is hidden with `#lab-content`; Home uses `#home-deploy`. Loses primary/red styling. |
| 24 | shell.lab-actions | Lab actions ▾ `#lab-actions` (injected before `#import-top`) | Page heading | Lab header › Lab actions ▾ › Lab › All lab operations…; Advanced › Lab operations › [All lab operations…] | All lab operations… | moved+renamed | operations.js:277 injects it next to `#import-top`, which now sits in Advanced (G1). |
| 25 | shell.empty-vs-lab-toggle | `#empty` vs `#lab-content` | App shell | App shell › three states: `#home` (labs exist, no activeId) · `#empty` (no labs) · `#lab-content` | — | behaviour-changed | Intentional: Home added, auto-select removed; add `.skeleton` until `state.loaded`. Precedence of activeId sources (G4). |
| 26 | shell.footer-version | v1.28.0 `#app-version` | Footer | UNMAPPED | Containerlab Node Manager v1.29.0 | UNMAPPED | Spec has no footer; app.js:43 writes `#app-version` unguarded (G2). |
| 27 | shell.footer-updated | Inventory updated {local time} / No lab selected `#updated` | Footer | UNMAPPED | Lab record updated <time> | UNMAPPED | Same footer removal (G2). |
| 28 | shell.toast | `#toast` (5 s auto-hide) | Global | Global › toast (bottom-right, dark rail) | — | unchanged | Message texts rewritten (§3). |
| 29 | empty.deploy | Deploy a new lab `#deploy-empty` | Empty panel | Home › Empty state (no labs) › [Deploy a new lab] | Deploy a new lab | unchanged | Gating by renderManagement kept; disabled title "Connect the VM first to browse its topologies". |
| 30 | empty.connect-vm | Connect the VM `#vm-connect-empty` | Empty panel (hidden until needed) | Home › Empty state › [Connect the VM] | Connect the VM | unchanged | |
| 31 | empty.vm-note | `#empty-vm-note` | Empty panel | Home › Empty state › status line | (sentence) | unchanged | |
| 32 | empty.discovered-list | Already running on the VM `#empty-discovered` | Empty panel | Home › Empty state › Labs already running on the VM › [Import] | Labs already running on the VM | unchanged | Click `[data-setup-name]` → importDiscovered kept. |
| 33 | empty.import-lab-definition | Import a lab definition `#import-empty` | Empty panel › "Have lab files instead?" | Home › Empty state › Have lab files instead? › [Import lab files…] (and `#home-import` when labs exist) | Import lab files… | renamed | Effective handler `openSetup()`. |
| 34 | empty.import-ansible-inventory | Import an Ansible inventory `#import-inventory-empty` | Empty panel | Home › Empty state › Have lab files instead? › [Import an Ansible inventory…] (and `#home-import-inventory`) | Import an Ansible inventory… | unchanged | Keep secondary; opens `#import-dialog`. |
| 35 | empty.platform-pills | Junos · IOS-XR · Arista EOS | Empty panel | UNMAPPED | Works with Junos, IOS-XR and Arista EOS | UNMAPPED | Spec silent; propose a caption under the empty state (G2). |
| 36 | deployment.status-text | Running / "2/2 discovered containers running…" / Last successful inspection / NOS ready · 2/2 / VM files: Updates available | Deployment bar | Advanced › Deployment details (raw, ids kept); student summary → Lab header `#lab-state` `#lab-ready` + banner via labState() | Running · 2 of 2 devices ready | reorganized | "VM files: Updates available" is not a banner (spec) but still drives `#sync-vm` visibility. |
| 37 | deployment.start | Start lab `#lab-start` | Deployment bar (primary) | Lab header › Lab actions ▾ › Lifecycle › Start lab (reads "Start stopped devices" when `quick.startAction==='start'`); Home › lab card › [Start lab]; banner [Start lab] | Start lab / Start stopped devices | moved | Gating + title preserved (`quick.canStart`); disabled menu items need an inline reason (G16). |
| 38 | deployment.destroy | Destroy lab `#lab-destroy` | Deployment bar (red outline, always visible) | Lab header › Lab actions ▾ › Danger › Destroy lab… | Destroy lab… | moved+renamed | Confirmation copy per spec §1.6 (says saved progress remains). |
| 39 | deployment.sync-vm | Sync from VM `#sync-vm` (hidden unless updates) | Deployment bar | Lab header › Lab actions ▾ › Lab › Sync topology from VM | Sync topology from VM | moved+renamed | management.js:157 restores literal 'Sync from VM' (G5). Toast → "Topology synced from the VM. Connections, credentials and backups kept." |
| 40 | deployment.update-yaml | Update lab YAML `#update-definition` | Deployment bar | Advanced › Lab source & deployment › [Replace topology file (YAML)…] | Replace topology file (YAML)… | moved+renamed | `openSetup(true)`. |
| 41 | deployment.link | Link deployment `#link-deployment` | Deployment bar | Advanced › Lab source & deployment › [Link to a running lab…]; banner "not linked" [Link to a running lab] | Link to a running lab… | moved+renamed | |
| 42 | deployment.remove-lab | Remove lab `#remove-lab` | Deployment bar | Lab header › Lab actions ▾ › Danger › Remove from this manager…; Advanced › Danger zone › [Remove this lab from the manager…] | Remove from this manager… | moved+renamed | management.js clears activeId + sessionStorage and sets `tab='inventory'` → shell must call `goHome()` (G3/G4). |
| 43 | metrics.node-count | Lab nodes `#node-count` | Metrics row | Advanced › Deployment details › key/value list | Devices | moved+renamed | Header `#lab-ready` shows the student version. |
| 44 | metrics.enabled-count | Selected for backup `#enabled-count` | Metrics row | Advanced › Deployment details › key/value list | Included in backups | moved+renamed | |
| 45 | metrics.ready-count | Credentials ready `#ready-count` | Metrics row | Advanced › Deployment details › key/value list | Ready to back up | moved+renamed | `readiness==='Ready'` ≠ ssh_ready; must not be shown as the "Ready" pill (G10). |
| 46 | metrics.schedule-summary | Backup schedule `#schedule-summary` (Manual / N min / Paused · N min) | Metrics row | Advanced › Deployment details › key/value list (and Tools › Configuration backups caption) | Automatic backups: Off / Every N min / Paused (lab not running) · every N min | moved+renamed | |
| 47 | git.destination | Connect a repository to save your lab progress `#git-open-settings` / `#git-destination` | Git progress bar | Progress › Status card › destination line | Saving to <repository> › <folder> / Not connected — choose a save location | moved+renamed | Title "Git repository settings" → "Save location settings". |
| 48 | git.progress-status | Capture configurations, commit and push in one step. `#git-progress-status` | Git progress bar | Progress › Status card › progressState sentence; Lab header `#lab-progress` | Saved to Git 12 minutes ago … | moved+renamed | Commit hash/UTC go under Details. |
| 49 | git.save-progress | Connect Git repository / Save progress `#git-save-progress` | Git progress bar (primary) | Lab header › [Save progress] (Progress tab `#progress-save` proxies it) | Save progress / Connect a save location… / Saving… | moved | git-progress.js:46/313 require `#git-progress-bar` to exist — keep the bar in the Progress tab DOM. Title → "Save every device's configuration to <repo> › <folder>". |
| 50 | git.save-menu | ▾ More save progress actions `#git-save-menu` (8 items) | Git progress bar | Lab header › split menu ▾ (aria-label "More save actions") | Save on this VM only · Create checkpoint… · Set baseline… · Saved versions & history · Load a saved version… · Upload saved progress · Update from the repository · Save location settings… | moved+renamed | `data-git-action` values unchanged; adopt `closeMenus()` Escape/outside-click. |
| 51 | tabs.switch | Topology · Nodes · Git repository · Backup history · More ▾ (Credentials, Action logs) | Control row | Lab workspace › Tabs Topology · Devices · Progress · Tools · Advanced (`showTab` maps inventory→devices, git→progress, backups→tools, credentials→advanced, logs→advanced) | Topology · Devices · Progress · Tools · Advanced | reorganized | `tab` must be normalised and a sub-view recorded, or active-tab state and log auto-refresh break (G3). `#extra-views` removed (guarded). |
| 52 | actions.grafana | Lab map in Grafana ↗ / Grafana ↗ `#grafana-open` | Control row actions | Tools › Telemetry card › [Open network dashboard ↗] | Open network dashboard ↗ (map: Open lab map ↗) | moved+renamed | `renderGrafanaLink` overwrites the label every poll (G6). |
| 53 | actions.capture-open | Capture packets `#capture-open` | Control row actions | Tools › Packet capture card › [Capture traffic]; Lab actions ▾ › Lab › Packet capture… (triggers it) | Capture traffic | moved+renamed | |
| 54 | actions.export-sessions | Export sessions `#export-sessions` | Control row actions | Tools › Export sessions card › [Export SuperPuTTY sessions…] | Export SuperPuTTY sessions… | moved+renamed | Card caption "A session file for PuTTY/SuperPuTTY with every device's address." |
| 55 | actions.test-nos-login | Test NOS login `#test` | Control row actions | Advanced › Lab operations › [Check device logins] | Check device logins | moved+renamed | Gating kept; title → "Add credentials for every device included in backups first". `startJob` jumps to `tab='backups'` (→ Tools) — teleports the user out of Advanced (G3). Toast → "Checking device logins…". |
| 56 | actions.backup-now | Back up now → `#backup` | Control row actions | Tools › Configuration backups card › [Back up now] | Back up now | moved | Same gating/title as 55. Toast → "Backing up configurations…". |
| 57 | topology.ssh-all | SSH all nodes ↗ `#map-ssh-all` | Topology view actions | Topology › map toolbar › More ▾ › Open all CLIs ↗; Tools › Open all CLIs card (own button, same function) | Open all CLIs ↗ | moved+renamed | Gating computed only for the id (G7). Titles → "Waiting for the devices to accept SSH — this becomes available automatically" / "No device is ready for a CLI yet". |
| 58 | topology.backup-all | Back up all configs `#map-backup-all` | Topology view actions | Topology › map toolbar › More ▾ › Back up all configurations | Back up all configurations | moved+renamed | Review dialog copy (management.js) → "2 of 2 devices can be backed up now. Devices that are unticked in the technical view are included too." / "These devices will be skipped:" / confirm "Back up 2 devices". |
| 59 | topology.import-map | Import topology `#import-map` (red primary) | Topology view actions | Topology › map toolbar › More ▾ › Import topology… | Import topology… | moved | Loses primary styling. |
| 60 | topology.map-tools | Expand map · Fit map · + · − · hint · Edit diagram | Topology view › map-tools row | Topology › overlay `.map-tools` (Fit, +, −, Expand, Edit diagram, More ▾) | Fit · + · − · Expand · Edit diagram · More ▾ | moved | Hint → "Click a device to open it. Right-click for more actions." Keep the wiring caveat in `#map-status`: "Links show the lab wiring, not live link state." |
| 61 | topology.map-status | `#map-status` | Topology view | Topology › caption under the map | 2 devices · 1 link | moved | Topology module rewrites "0 unmatched. Unmatched nodes have no connection actions." |
| 62 | topology.node-context-menu | Node actions `#node-context-menu` | Topology › right-click | Topology › right-click menu (Open CLI ↗ · Capture traffic… · Back up configuration · Device details) | (items) | renamed | Left-click now opens the drawer; uses `sshHint`/`openDetails` from app.js. |
| 63 | nodes.search | Filter nodes… `#search` | Nodes view | Devices › search field (filters `.device-row` list and technical table) | Find a device… | unchanged | `renderDeviceList()` must apply the same filter; `selectLab` still clears it. |
| 64 | nodes.caption | `{lab.source} · N selected` `#inventory-caption` | Nodes view | Devices › Technical view › caption | Source: clabllm-dev.clab.yaml · 2 of 2 included in backups | moved+renamed | Raw `lab.source` gets a label. |
| 65 | nodes.row | table rows `tbody#nodes` | Nodes view | Devices › `.device-row` list (name, platform, state pill, explanation, Open CLI, Details) + Technical view table | — | reorganized | Empty row → "No devices match. Try another name, address or platform." |
| 66 | nodes.include-checkbox | Include {name} `data-enable` | Nodes table col 1 | Devices › Technical view › checkbox; Device workspace › Advanced › Included in backups | Include {name} in backups | reorganized | `change` delegation is on `#nodes` only (G8); disabled-without-platform needs a reason. |
| 67 | nodes.name-button | {short_name or name} + endpoint `button.node-name[data-details]` | Nodes table | Devices › list row name; Technical view | — | unchanged | |
| 68 | nodes.platform-badge | platform badge + credential label | Nodes table | Devices › list row platform; Technical view | Junos · Default lab login / From the lab files / No credentials yet | renamed | profileName(): "Containerlab default login" → "Default lab login", "From inventory" → "From the lab files", "Not configured" → "No credentials yet". |
| 69 | nodes.last-ssh-check | reachable / unreachable / Not checked · Run a login check | Nodes table | Devices › Technical view › Last login check | Login OK / Login failed / Not checked · No check yet | renamed | `badge()` prints raw words (G11). |
| 70 | nodes.last-backup | succeeded / No backup yet | Nodes table | Devices › Technical view › Last backup | Succeeded / No backup yet | unchanged | Via badgeLabel (G11). |
| 71 | node-actions.capture | Capture `data-capture` | Row, drawer, context menu | Devices row (technical), drawer primary row, context menu | Capture traffic | renamed | Disabled title → "Packet capture is not set up on this VM yet. See Tools › Packet capture." |
| 72 | node-actions.ssh | SSH ↗ `data-terminal` | Row, drawer, context menu | Devices › row [Open CLI ↗] (primary); drawer primary; context menu; device rail | Open CLI ↗ | renamed | sshHint → deviceState.detail: "RTR1 is still starting. The CLI opens automatically when it answers." / "RTR3 is running, but SSH login failed with the saved credentials. Check credentials." / "RTR2 is not running." / "Add login credentials to open the CLI." Inline text, not only title. |
| 73 | node-actions.backup | Back up `data-backup` | Row, drawer | Technical view row; drawer primary row; context menu | Back up configuration | renamed | Disabled title → "Available when the device has a supported network OS and credentials and no other backup is running." |
| 74 | node-actions.details | Details → `data-details` | Nodes table | Devices › row [Details]; context menu "Device details" | Details | unchanged | |
| 75 | node-actions.test-login | Test login `data-check` (drawer) | Drawer head actions | Device workspace › Advanced › [Test login] | Test login | moved | Disabled has no title today → "Add credentials first". Placement needs a second container (G9). |
| 76 | node-actions.edit-connection | Edit connection `data-edit` (drawer) | Drawer head actions | Device workspace › Advanced › [Edit connection…]; deviceState "Check credentials" next action | Edit connection… | moved+renamed | (G9). |
| 77 | nodes.notice | "Checkboxes select nodes for lab-wide and scheduled backups…" | Nodes view | Devices › Technical view › caption | Ticked devices are included in lab-wide and scheduled backups. Login checks run on demand; the time shows when access was last verified. | moved+renamed | |
| 78 | credentials.add | + Add credentials `#add-profile` | Credentials view | Advanced › Credentials › [Add credentials]; deviceState "Add credentials" next action | Add credentials | moved | `openProfile()` no-ops without a lab — fine, Advanced exists only on a lab. |
| 79 | credentials.card | profile cards `#profiles` | Credentials view | Advanced › Credentials › cards | Password / SSH key · Default for Junos | moved | Empty state → "No login credentials yet" / "Add one profile per network OS. Credentials found in the lab files work automatically." |
| 80 | credentials.notice | "Credentials already present in the inventory…" | Credentials view | Advanced › Credentials › intro | Credentials found in the lab files are used automatically. An assigned profile takes precedence. Passwords are never shown after saving. | moved+renamed | |
| 81 | backups.schedule-form | Every [n] minutes [Save] 0 = manual `#schedule-form` | Backup history view | Tools › Configuration backups card › schedule row | Back up automatically every [n] minutes (0 = only when I ask) [Save] | moved+renamed | `#interval` focus rule kept. |
| 82 | backups.job-accordion | Configuration backup / NOS login test · automatic `#jobs` | Backup history view | Tools › Configuration backups card › job list | Configuration backup / Login check (automatic) · <relative time> · N devices | moved+renamed | 25+ flat cards remain; propose latest 5 + [Show all]. UTC stays inside the expanded body. |
| 83 | backups.view-action-logs | View action logs `data-logs` | Job body | Tools › job › [View action logs] → Advanced › Action logs with `#log-job` prefilled | View action logs | moved | Works through `showTab('logs')` mapping (G3). |
| 84 | backups.job-node-result | per-node badge + message + Captured … (historical job timestamp) | Job body | Tools › job body | Captured <UTC> (time taken from the backup job) | unchanged | |
| 85 | backups.download-config | ↓ Download config | Job body | Tools › job body | Download configuration | unchanged | |
| 86 | backups.download-zip | ↓ Download all (ZIP) | Job body | Tools › job body | Download all as ZIP | unchanged | |
| 87 | details.open | NODE WORKSPACE drawer `#details-dialog` | Drawer (dark head) | Device workspace `dialog#details-dialog.drawer` › head: eyebrow "Device", name, platform · state pill · endpoint | Device | reorganized | Opened by left-click on the map, list name/Details, context menu, hash `device=`. |
| 88 | details.actions | Capture · SSH ↗ · Back up · Test login · Edit connection `#details-actions` | Drawer head | Device workspace › primary row [Open CLI ↗] [Capture traffic] [Back up configuration]; Test login / Edit connection… → Advanced section | (see 71–76) | reorganized | Single `_markup`-diffed container cannot be split by CSS (G9). |
| 89 | details.connection-settings | Connection settings dl | Drawer body | Device workspace › Advanced `<details>` › Connection settings `dl.health-grid` (+ include checkbox) | Name in lab files · Network OS · Login credentials · Included in backups (checkbox) · Can be backed up: Yes/No | moved+renamed | "Choose NOS" fallback → "No — choose a network OS" (G10). |
| 90 | details.last-ssh-check | Last SSH check (badge, message, time, help) | Drawer body | Device workspace › Status section: deviceState sentence + next action; Details expander with raw `nos_login.message`, time, "automatic check" | Status | reorganized | Help → "Running devices are checked automatically until they accept a login. Test login (under Advanced) checks the saved credentials now." |
| 91 | details.configuration-history | Configuration history `#node-history` | Drawer body | Device workspace › Configuration history | Latest backup / Backup · [Download configuration] | unchanged | Empty → "No configuration backups for this device yet. Back up configuration creates one." |
| 92 | details.close | × Close node details | Drawer head | Device workspace › × (aria-label "Close device details") | Close device details | unchanged | Native Esc kept (`showModal`). |
| 93 | logs.filters | Scope · Level · Node · Job ID | Action logs view | Advanced › Action logs › filters | Scope (This lab / All manager actions) · Level · Device · Job ID | moved | Values `lab`/`all`, level values unchanged. |
| 94 | logs.refresh | Refresh `#refresh-logs` | Action logs view | Advanced › Action logs › [Refresh] | Refresh | moved | |
| 95 | logs.table | UTC time / level · Node / job · Action · Details | Action logs view | Advanced › Action logs › table | Time (UTC) / level · Device / job · Action · Details; "Worker" → "Manager" | moved | |
| 96 | logs.status | "{n} events shown … Updated …" / "Could not refresh logs: …" `#log-status` | Action logs view | Advanced › Action logs › status line | "{n} events shown (latest 1,000 matching). Updated 16:17." / "The logs could not be refreshed: …" | moved | |
| 97 | logs.download | Download logs `#download-logs` | Action logs view | Advanced › Action logs › [Download logs] | Download logs | moved | Filename `backup-action-logs.jsonl` kept (technical artefact). |
| 98 | logs.auto-refresh | every 4 s while `tab==='logs'` | Poll | Advanced › Action logs (while the Advanced tab shows the logs panel) | — | behaviour-changed | With logs→advanced mapping `tab==='logs'` is never true → auto-refresh silently stops (G3). |
| 99 | dialog.import | Import a lab `#import-dialog` (Ansible inventory) | opened from sidebar/empty/heading | Home › [Import an Ansible inventory…]; Advanced › Lab source & deployment › [Replace device list…] | Import a lab from an Ansible inventory / Replace this lab's device list | moved | Copy rewrite §2. FormData names unchanged. |
| 100 | dialog.profile | Add credentials `#profile-dialog` | Credentials view | Advanced › Credentials › [Add credentials] | Add login credentials | moved | Copy rewrite §2. |
| 101 | dialog.node | Edit node `#node-dialog` | Drawer › Edit connection | Device workspace › Advanced › [Edit connection…] | {name} · Connection | moved | Copy rewrite §2; disabled "Address source" needs a visible reason. |
| 102 | dialog.close-buttons | `.close` closes closest dialog | Global | Global (dialogs) + `closeMenus()` for `details.menu` | — | unchanged | New dialogs/menus must be bound at load. |
| 103 | shell.initial-load | `refresh()` once | Lifecycle | Lifecycle + `.skeleton` on Home until `state.loaded` | — | unchanged | |
| 104 | shell.poll-state | 4 s `/api/state` + `/health` per render | Lifecycle | Lifecycle (unchanged cadence; `refreshHealth` already skips when `activeId===''`) | — | unchanged | No new polling loops (spec §0.8). |
| 105 | shell.active-lab-persistence | `sessionStorage.activeLab`; first lab auto-selected | Lifecycle | Lifecycle › hash `#lab=…&view=…&device=…` + sessionStorage + localStorage `clab.lastLab` | — | behaviour-changed | Precedence unspecified (G4). |

## 2. Dialogs (app.js-owned) — student-facing rewrite

### `#import-dialog` (Ansible inventory)
- Eyebrow "INVENTORY UPLOAD" → "Import". Title "Import a lab" → "Import a lab from an Ansible inventory"; replace mode "Replace lab inventory" → "Replace this lab's device list".
- Intro "Use containerlab's generated Ansible inventory. No node addresses are built into this image." → "Upload the `ansible-inventory.yml` that containerlab generates next to the lab. It lists every device and its management address."
- "Ansible inventory required" → "Inventory file" with help "YAML or JSON · up to 1 MiB".
- "Optional: topology metadata" → "Optional: topology-data.json" with help "Only needed when the inventory does not say what kind of device each node is."
- Submit "Import and review" → "Import lab". Toast "Inventory imported. Review the nodes and credentials." → "Lab imported. Check the devices and their login credentials."

### `#profile-dialog` (credentials)
- Eyebrow "NOS AUTHENTICATION" → "Credentials". Title "Add credentials" → "Add login credentials". Intro → "The username and password (or SSH key) used to log in to the device CLI."
- "NOS username/password" → "Username" / "Password". "Generic SSH / Linux (terminal only)" → "Linux host (CLI only, no backups)". "EOS enable password" keep; help → "Arista EOS enters enable mode before reading the configuration. Leave blank if the account does not need it."
- Checkbox "Use as the default for this NOS in this lab" → "Use for every {NOS} device in this lab that has no other profile". Toast "NOS credentials saved." → "Credentials saved."

### `#node-dialog` (connection)
- Eyebrow "NODE CONNECTION" → "Connection". Title "Edit node" → "{name} · Connection".
- "Download device name optional" → "Short name (used in backup file names)"; help → "Use a short name such as GTW-1 when the full name carries a long lab prefix. New backups use it."
- "NOS management IP or DNS" → "Management address (IP or hostname)". "Address source": "Manual address and port" → "Enter manually"; "Automatic from VM discovery" → "Use the address the running lab reports"; help → "Automatic uses the management address the running lab reports and port 22. Manual keeps the address you type." When the select is disabled (`!lab.deployment_name`) show "Automatic is available once this lab is linked to a running lab."
- "Credential profile" → "Login credentials"; option "NOS default / inventory credentials" → "Lab default (lab files or the default profile for this OS)". "Include in backups" keep.

### `#details-dialog` (device workspace)
- Eyebrow "NODE WORKSPACE" → "Device"; close aria-label "Close device details". Sections per §1.4; raw `nos_login.message` and "automatic readiness check" under Details.

## 3. Microcopy rewrites for every item in inventory §12 that the spec does not already cover

| Current | Where | Rewrite |
|---|---|---|
| Worker idle / SSH job in progress / Saving lab progress | `#worker-state` | hidden / "Backing up…" or "Checking logins…" (by job operation) / "Saving progress…" |
| Complete credentials for enabled nodes | `#test`/`#backup` title | "Add credentials for every device included in backups first" |
| Requires a supported NOS, credentials, and an idle worker | row Back up title | "Available when the device has a supported network OS and credentials and no other backup is running" |
| Waiting for the NOS to accept SSH logins; this opens automatically / No node is ready for SSH | `#map-ssh-all` title | "Waiting for the devices to accept SSH — this becomes available automatically" / "No device is ready for a CLI yet" |
| NOS is still booting; SSH opens when it accepts a login | sshHint booting | "{Name} is still starting. The CLI opens automatically when it answers." |
| SSH login failed with the saved credentials; assign a credential profile | sshHint failed | "{Name} is running, but SSH login failed with the saved credentials. Check credentials." |
| Node is not running / Assign credentials first | sshHint | "{Name} is not running." / "Add login credentials to open the CLI." |
| Packet capture is not enabled. Open Capture packets for setup. | capture attrs | "Packet capture is not set up on this VM yet. See Tools › Packet capture." |
| Testing NOS login with show version. / Backup started. | toasts | "Checking device logins…" / "Backing up configurations…" |
| Configuration backup started. View progress in Backup history. | backup-all toast | "Backing up configurations. Progress is under Tools › Configuration backups." |
| Check the form fields and try again. / Request failed | api() fallbacks | keep first; "Request failed" → "The manager did not respond. Try again." |
| reachable / unreachable / succeeded / failed / interrupted / partial / queued | badge() | Login OK / Login failed / Succeeded / Failed / Interrupted / Partly succeeded / Queued (G11) |
| Containerlab default login / From inventory / Not configured | profileName | Default lab login / From the lab files / No credentials yet |
| Choose NOS | drawer backup readiness | "No — choose a network OS" |
| Included in lab backups / Excluded from lab backups | drawer | checkbox "Included in backups" |
| Latest successful backup / Backup | node history | Latest backup / Backup |
| NOS login test · automatic / Configuration backup | job title | Login check (automatic) / Configuration backup |
| (historical job timestamp) | job node | (time taken from the backup job) |
| No jobs yet / Test the NOS login, then run a backup… | jobs empty state | "No backups yet" / "Back up now saves a copy of every device's configuration on this VM." |
| Unlinked (lab list sub-text) | `#labs` | Not linked (labState) |
| Manual / Paused · N min | `#schedule-summary` | Off / Paused (lab not running) · every N min |
| All worker actions / Worker / Node / job | logs | All manager actions / Manager / Device / job |
| Could not refresh logs: … | `#log-status` | "The logs could not be refreshed: …" |
| Time unavailable | utcDisplay | "Unknown time" |
| Inventory updated <time> / No lab selected | `#updated` | "Lab record updated <time>" (Advanced › Technical details) |
| Your next lab starts here. / Deploy a topology from your VM, or pick up a lab that is already running. | `#title`/`#subtitle` (no lab) | Home heading "My labs"; subtitle removed |
| Import your nodes. Connect, experiment, and keep your configurations close. | HTML default subtitle | removed |
| Supported device types as of release 1.28.0 | `#supported-release` | "Works with Junos, IOS-XR and Arista EOS · v1.29.0" |
| No file results yet. Refresh discovery. If using an older helper, update it on the VM. | discovery file list | "Nothing found yet. Use Manager › Refresh lab list. If the VM helper is old, update it on the VM." |
| Import again · Right-click to clear exclusion | excluded labs | "Not imported automatically" + visible [Import again] [Allow again] |
| Try importing VM files / Ready to import · confirmation required | discovered labs | "Import" / "Ready to import" |
| Discovery updated. / Configure the VM connection first. / Discovery is paused or still checking. | `#vm-refresh` toasts | "Lab list refreshed." / "Connect the VM first." / "The VM check is paused or still running." |

## 4. Spec gaps (each breaks a capability or its gating unless fixed)

- **G1 — shell.lab-actions (24)**: operations.js:276-277 injects `#lab-actions` `beforebegin` `#import-top`; the spec moves `#import-top` into Advanced › Lab source, so "All lab operations…" lands in Advanced and never appears in Lab actions ▾. Fix: declare `<button id="lab-actions" hidden>` statically inside the Lab actions ▾ menu (Lab group) in index.html; operations.js injects only `if($('import-top')&&!$('lab-actions'))`; the Advanced › Lab operations entry is a second button `[data-open-operations]` → `openLabOperations()`.
- **G2 — shell.subtitle (22), footer-version (26), footer-updated (27), supported-release (16), platform-pills (35)**: render() writes `#subtitle`, `#app-version`, `#updated` unguarded (app.js:43/48/50) and the spec removes the footer and the subtitle → a TypeError at first render aborts the remaining bindings (inventory risk 3). Fix: Manager ▾ footer line `<span id="app-version">` + `<span id="supported-release">` rendered as "Containerlab Node Manager v1.29.0 · Works with Junos, IOS-XR and Arista EOS"; `#updated` becomes the `<dd>` of an Advanced › Technical details row "Lab record updated"; delete `#subtitle` and its two writes; the empty state gets a caption "Works with Junos, IOS-XR and Arista EOS" in place of the pills.
- **G3 — tabs.switch (51), logs.auto-refresh (98), actions.test-nos-login (55), remove-lab (42)**: legacy names are assigned straight to `tab` (app.js:117 `'inventory'`, :122 `'backups'`, management.js remove → `'inventory'`; git-progress calls `showTab('git')`). The spec maps names inside showTab but never says `tab` is normalised: `[data-tab]` active state compares to `tab`, and the poll's `if(tab==='logs')refreshLogs()` is never true once logs→advanced, so Action logs stop auto-refreshing. Fix: `showTab(value){tab=TAB_ALIAS[value]||value; subview=SUBVIEW[value]||null; …}`; poll condition `tab==='advanced'&&subview==='logs'`; `startJob` and the import handler call `showTab('backups')`/`showTab('inventory')` instead of assigning; after "Check device logins" from Advanced stay on Advanced and toast "Checking device logins… results appear under Tools › Configuration backups"; remove-lab → `goHome()`.
- **G4 — shell.active-lab-persistence (105), empty-vs-lab-toggle (25)**: three sources of the active lab (hash `#lab=`, `sessionStorage.activeLab` seeded at app.js:4, localStorage `clab.lastLab`) with no precedence; Journey 1 expects `/` to show Home with "Continue…", but a same-tab reload with sessionStorage set opens the lab directly. Fix: precedence hash > sessionStorage (same-tab reload resumes the lab) > Home; a new tab has no sessionStorage → Home with the Continue card from localStorage; `goHome()` clears sessionStorage.activeLab and the hash.
- **G5 — refresh-discovery (8), sync-vm (39)**: management.js:146 and :157 restore the literal labels 'Refresh discovery' / 'Sync from VM' after the busy state, so the relabelled items revert after first use. Fix: idle label from `button.dataset.label` (set in index.html) or a constant; busy labels "Checking the VM…" / "Syncing…".
- **G6 — actions.grafana (52)**: `renderGrafanaLink` (app.js:37) rewrites `#grafana-open` text and title on every 4 s render, so "Open network dashboard" cannot survive a poll. Fix: renderGrafanaLink emits "Open network dashboard ↗" (with `map_uid`: "Open lab map ↗"); the technical sentence moves to a caption `#grafana-caption` ("Live interface rates, link state and BGP neighbours. Grafana starts on the VM when needed."); no title.
- **G7 — topology.ssh-all (57)**: disabled/title gating (app.js:57-58) and the click (management.js:226) are bound to the `#map-ssh-all` id only; the Tools "Open all CLIs" button the spec adds would be ungated. Fix: render() applies the same `disabled`/`title` to every `[data-ssh-all]` button and management.js binds on `[data-ssh-all]`; the Tools card shows the reason inline ("No device is ready for a CLI yet").
- **G8 — nodes.include-checkbox (66), details.connection-settings (89)**: the include-in-backups `change` handler is delegated on `#nodes` only (app.js:116); the spec's drawer checkbox would do nothing, and `renderDetails` rewrites `#details-info` every poll. Fix: extract `handleEnableChange(e)` and bind it on `#nodes` and `#details-info`; after the PUT `refresh()` re-renders both; disabled (no platform) checkbox gets `title="Choose a network OS first (Edit connection)"`.
- **G9 — details.actions (88), test-login (75), edit-connection (76)**: `nodeActions(n,true)` returns one string rendered into the `_markup`-diffed `#details-actions` in the drawer head; CSS cannot move `[data-check]`/`[data-edit]` into the Advanced `<details>` at the bottom. Fix: optional third parameter `nodeActions(n, details=false, group='all')` returning 'primary' (capture/terminal/backup) or 'advanced' (check/edit); `renderDetails` writes the advanced group into `#details-advanced-actions` inside the Advanced section; app.js binds `handleNodeAction` on that container too. Existing calls and tests keep the full return.
- **G10 — metrics.ready-count (45), details.connection-settings (89), node-actions.backup (73)**: backend `readiness==='Ready'` means "supported NOS + credentials" (backup eligibility) while spec §2 uses "Ready" for `ssh_ready`; two different facts would carry the same word on one screen. Fix: student-facing label for `readiness` is "Can be backed up: Yes/No" and the Advanced kv row "Ready to back up: n of m"; never route `readiness` through the `.pill` vocabulary; raw value under Details.
- **G11 — nodes.last-ssh-check (69), backups.job-accordion (82), badge()**: `badge(status)` prints raw backend words (reachable, unreachable, succeeded, interrupted, partial, queued) in the technical table and job list, which students still see. Fix: `badgeLabel(status)` in status.js used by `badge()` (reachable→Login OK, unreachable→Login failed, succeeded→Succeeded, failed→Failed, interrupted→Interrupted, partial→Partly succeeded, queued→Queued, running→Running); class mapping unchanged; raw value in `title`.
- **G12 — sidebar.deploy-new-lab (5)**: two flows share the label "Deploy a new lab…": Manager ▾ `#vm-projects` navigates to the full-page `/static/workspace.html#mode=folder`; Home `#home-deploy` calls `openDeploy()` (in-page browse dialog). Fix: both Home and Manager use `openDeploy()` under "Deploy a new lab…"; the full-page browser stays reachable as Manager ▾ "Lab topologies on the VM…" (`#vm-projects`).
- **G13 — operation-summary (11), worker-state (20)**: `#operation-summary` moves into the lab banner, but operations not bound to a lab (deploy from the browser, inspect-all) report there on every screen today; Home has no slot. `#worker-state` "Restoring…" needs a restore-job field that `busy()` does not read and the spec does not name. Fix: Home gets `#home-banner` fed by `#operation-summary` when `activeId===''`; the spec names the restore job source (the field restore.js already polls) so `#worker-state` and `labState` read it without a new poll.
- **G14 — sidebar.lab-context-menu (4)**: right-click/Shift+F10 → Lab actions is bound on `#labs` only; Home lab cards lose it. Fix: generalise operations.js:282-283 to `#home` (same `[data-lab]` selector) or add a ghost "…" button per card → `openLabOperations(id)`.
- **G15 — sidebar.lab-list (2)**: the lab switcher body reuses render()'s `#labs` markup whose sub-text is `{n} nodes · {deployment.status||'Unlinked'}` — raw backend status in a student-facing menu. Fix: render() emits `<small>${labState(lab).label} · ${ready} of ${total} devices ready</small>`; keep `.lab-item`, `data-lab`, `★` prefix and favourites-first ordering.
- **G16 — deployment.start (37)**: inside `details.menu` the `disabled`+`title` gating of `#lab-start` gives a greyed item with no visible reason (tooltips on disabled buttons in menus are unreliable). Fix: menu items render `<small class="menu-reason">` with the title text when disabled ("Already running" / "Deploying…"), using the existing `quick.startAction` logic.
- **G17 — Advanced panel naming (23, 40, 41)**: spec §1.2 says "Advanced › Lab source", §1.3 says "Deployment details" for the same panel. Fix: one panel "Lab source & deployment" with intro "Where this lab's files come from and how it is linked to the running lab."

## 5. Summary counts

unchanged 18 · moved 23 · renamed 9 · moved+renamed 38 · reorganized 9 · behaviour-changed 4 · UNMAPPED 4 (22, 26, 27, 35 — nothing is lost; all four have a proposed home in G2). Intentionally removed: none.
