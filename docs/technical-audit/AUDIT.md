# Technical audit: the record

One record for the audit that started on 2026-09-23 from `main` `b1ced1d` (1.30.38). Resumable state is in
[PICKUP.md](PICKUP.md); the preservation matrix is [FEATURE-PARITY.md](FEATURE-PARITY.md); the telemetry
retirement is traced in [TELEMETRY-REMOVAL.md](TELEMETRY-REMOVAL.md); what was run is in
[VALIDATION.md](VALIDATION.md). This folder follows the version rules of
[repository maintenance](../REPOSITORY-MAINTENANCE.md).

## 1. Scope and coverage

Every first-party module, script, configuration group, test group and documentation family gets one of:
**reviewed** (read, findings recorded), **reviewed/no change**, **changed** (with the chunk), **blocked** (with the
reason). Filled in as the audits report.

| Area | Paths | Status |
|---|---|---|
| Architecture and ownership | `app/main.py`, module boundaries | reviewed (AU1): lifecycle order and the three NDJSON readers consistent; B-006 |
| Backend behaviour | `runner.py`, `node_services.py`, `node_readiness.py`, `discovery.py`, `vm_files.py`, `lab_operations.py`, `diagnostics.py`, `downloads.py`, `inventory.py`, `capture*.py`, `textdiff.py`, `layout.py`, `topology.py`, `drawio_export.py` | reviewed (AU1, Sonnet, read-only): findings B-001 to B-006 |
| State and concurrency | `store.py`, `runner.py` pool, `operation_busy`, `restore.py` pools, `git_progress.py` pending jobs | reviewed (AU1): B-001, B-003, B-005 |
| Frontend | `app/static/*.js`, `*.html`, `style.css` | reviewed (AU2, Sonnet, read-only): F-001 to F-006; the cross-file function map is in the AU2 report (scratch) |
| Editor integration | `lab-builder/src/main.tsx`, `lab-builder-page.js`, `lab-builder-yaml.js`, `map-editor-page.js` | reviewed/no change (AU2): whitelist, undo ownership, drafts, build reproducibility consistent |
| Git and filesystem | `host_git.py`, `git_progress.py`, `host_operations.py`, `host_files.py`, `deploy/setup-git.sh`, `git-onboard.py` | reviewed (AU3, Sonnet, read-only): S-004; the Git save flow and the helper write pattern consistent |
| Network drivers | `restore_*.py`, `runner.py`, `node_readiness.py`, `inventory.py` defaults | reviewed/no change (AU1): the three drivers, the IOS XR arming session, credential precedence and readiness consistent; B-004 |
| Install, build, release | `deploy/*.sh`, `install-manager.py`, `check_*.py`, `apt_*.py`, Dockerfile, compose files, `verify-release.py`, `set-release.py` | reviewed (AU3): S-001, S-003, S-005; changed in chunk 1 (telemetry) |
| Dependencies | `requirements.txt`, `collections.yml`, `lab-builder/package.json`, vendored assets | reviewed (AU3): every installed Python pin clean on OSV except paramiko's disputed SHA-1 advisory (S-006); the editor lockfile carries S-002; `pygnmi` removed in chunk 1 |
| Tests and CI | `tests/`, `.github/workflows/release-check.yml` | reviewed (AU3, AU4): CI runs every test file except the opt-in EOS fixture; T-001 to T-005; fixtures and fakes agree with the real helpers' shapes |
| Security boundaries | `main.py` guard, WebSockets, helpers, gateway, `capture_service.py` | reviewed (AU1, AU3): B-002 (scrub after truncation); the gateway's command list, sudoers, the capture service's fixed image and the WebSocket checks consistent |
| Performance and resource use | polling loops, startup, image size | reviewed (AU1): B-001; the polling intervals are bounded and documented |
| Documentation | `README.md`, `docs/`, `CLAUDE.md`, `deploy/*.md`, `NODE-FEATURES.md` | reviewed (AU4, Sonnet, read-only): every command, path, variable and numeric claim checked matched the code except D-001 to D-004; changed in chunk 1 (telemetry) |
| Telemetry and Grafana | see TELEMETRY-REMOVAL.md | changed (chunk 1, R-001) |

## 2. Findings

Columns: ID, paths and symbols, observed problem, evidence or reproducer, impact, priority, remedy,
compatibility risk, validation, owner, disposition. Confirmed defects are marked **defect**; hypotheses and
preferences are marked as such. Filled in as the audits report.

| ID | Paths | Problem | Evidence | Impact | Priority | Remedy | Compatibility | Validation | Owner | Disposition |
|---|---|---|---|---|---|---|---|---|---|---|
| A-001 | `app/main.py` guard | **defect**, cosmetic: the audit-log arrow is mojibake (`â†’`) | `store.event('api.request', … â†’ …)`; carried from the maintenance audit §5 | garbled log lines | low | write the arrow as `->` or the real character | none | `test_logging.py` | chunk 2 (backend) | closed in 1.30.42 |
| B-001 | `runner.py` (`jobs.insert`), `git_progress.py` (`git_jobs.append`), `restore.py` (`restore_jobs.append`), `main.py` `/api/state` (`jobs` unsliced), `store.py` `save()` | **defect**: the `jobs`, `git_jobs` and `restore_jobs` lists grow without bound (only `operations` is trimmed to 200); every save re-encrypts the whole document under the single lock | AU1 reproducer `repro_state_growth.py`: `save()` 0.15 ms → ~200 ms and `/api/state` jobs ~585 ms at 10,000 jobs | slow saves and state reads on a long-lived manager | high | trim each list on write like `operations` (keep the newest N, never a job that is queued, running or in flight); slice the `/api/state` jobs read | stored jobs beyond the cap disappear from history (downloads of older backups: keep the files, decide the cap) | unit test per list + `test_downloads.py` | chunk 2 (backend) | closed in 1.30.42 |
| B-002 | `lab_operations.py` `execute()` output buffer, `scrub()` | **defect**: the streamed VM-operation output is truncated to its last 512 KiB before `scrub()` runs, so a secret cut by the window survives as a fragment; `/api/operations/{id}` returns the buffer | AU1 reproducer `repro_scrub_truncate3.py` (48 of 49 password characters survive) | a VM password fragment can reach the browser | high | scrub each chunk before appending (or keep a small overlap so a secret spanning the cut is still matched) | none | unit test with a secret at the boundary | chunk 2 (backend) | closed in 1.30.42 |
| B-003 | `runner.py` `Runner.close()` | **defect**: the scheduler thread is never joined (Discovery and ReadinessMonitor join theirs) | AU1 reproducer `repro_scheduler_thread.py` | threads outlive `close()` by up to 2 s (tests, shutdown) | medium | join with a timeout like the others | none | `test_runner_resilience.py` | chunk 2 (backend) | closed in 1.30.42 |
| B-004 | `runner.py` post-backup `git` subprocesses | hypothesis: no timeout, unlike the bounded ansible run | reading | a hung `git` blocks the one-worker backup pipeline | medium | add a timeout and the same failure path | none | unit test with a fake `git` | chunk 2 (backend) | closed in 1.30.42 |
| B-005 | `lab_operations.py` `execute()` `finally` block | hypothesis: one read of `store.state['operations']` outside `store.lock` with a bare `next()` | reading | breaks the single-lock discipline; `StopIteration` if the entry vanished | low | take the lock and default the lookup | none | existing suite | chunk 2 (backend) | closed in 1.30.42 |
| B-006 | late imports in `runner.py`, `node_readiness.py`, `main.py` | preference: three late imports avoid no cycle. Correction found while fixing: `runner.py`'s import of `lab_operations` does guard a real cycle (`runner → lab_operations → topology → runner`, `topology.py` imports `effective_credentials`); the AU1 import table missed it | import graph; the cycle reproduced by moving it | readability | low | move the two that avoid no cycle to module level; keep the third with a comment | none | import check | chunk 2 (backend) | closed in 1.30.42 |
| F-001 | `index.html` `#i-star`, `home.js`, `app.js` favourite glyph | **defect**: the star symbol lacks `fill="none"` so it renders solid in both states (the addendum says filled only when pressed) | AU2 reading of the sprite; no rendered test | favourite state shown by colour only | medium | add `fill="none"` to the outline path like the sibling symbols | none | `test_home_ui.js` markup claim | chunk 3 | closed in 1.30.40 |
| F-002 | `style.css` `.git-diff-columns`, `.git-diff-file`, `.git-repository-card` | **defect**: dead CSS of the pre-redesign diff layout | AU2 consumer search over scripts, HTML, tests, tools | maintenance | low | delete the rules | none | grep + `verify_after.py` | chunk 3 | closed in 1.30.40 |
| F-003 | `capture-session.js` | **defect**: the only static script without `'use strict'` | AU2 mechanical check | house style, silent globals | low | add the directive | none | `node --check` | chunk 3 | closed in 1.30.40 |
| F-004 | `git-progress.js` `gitWhen`, `git-places.js` `gitPlacesWhen` | **defect**: byte-identical duplicate; `git-places.js` never loads without `git-progress.js` | AU2 load-order check | duplication | low | delete `gitPlacesWhen`, use `gitWhen` | none | `test_git_places_ui.js` | chunk 3 | closed in 1.30.40 |
| F-005 | `style.css` `.topology-wire path` | hypothesis: the base stroke rule does not exclude `path.capture-hit` (the invariant in CLAUDE.md); correct today only by cascade order | AU2 reading | fragile | low | add `:not(.capture-hit)` | none | `test_topology_ui.js`, `verify_after.py` | chunk 3 | closed in 1.30.40 |
| F-006 | `index.html` `#subtitle` | preference: dead hidden element the addendum said to delete | grep | none | low | delete the element | none | `test_shell_ui.js` | chunk 3 | closed in 1.30.40 |
| S-001 | `install-manager.py` lazydocker download | **defect**: the tarball is installed without checksum verification although upstream publishes `checksums.txt` and `install-prerequisites.sh` verifies containerlab the same way | AU3 reading and the live release assets | an unverified binary on `PATH` (low likelihood) | medium | fetch `checksums.txt` and verify before installing; fail as a warning like the rest of the optional step | none | `test_install_manager.py` | chunk 4 | closed in 1.30.41 |
| S-002 | `lab-builder/package-lock.json` (`maplibre-gl` via `@containerlab/clab-ui` 0.3.2; `dompurify` via `monaco-editor`; `markdown-it`) | dependency: `npm audit` reports one critical unfixed advisory (GHSA-jrc7-96c5-q579, maplibre-gl ≤ 6.4.0) and three moderate ones, all transitive inside the exactly pinned editor | AU3 `npm audit --package-lock-only` | the editor bundle ships vulnerable code paths (Geo layout, Markdown) that the builder hides | high to decide | evaluate an `overrides` entry or a newer `@containerlab/clab-ui`; needs Node 24, `npm ci`, `node build.mjs --check`, the builder tests and a release (the bundle is cached immutable) | bundle change | `test_lab_builder_ui.js`, `student_workflow.py`, CI rebuild | chunk 4 | closed in 1.30.41 |
| S-003 | both Dockerfiles, `requirements.txt` | preference: base image by tag, no pip hashes | AU3 reading | reproducibility | low | digest-pin the base image; consider `--require-hashes` | build | build | deferred (see §3) | open |
| S-004 | `host_git.py` per-repository state directory (`chmod`, lock `open`, message file) | hypothesis: touched by path after `drop_owner()`, unlike the descriptor pattern elsewhere; no privilege boundary crossed | AU3 reading | inconsistency | low | open the state directory once and work relative to it | helper change, review-grade | `test_host_git.py` + live save | deferred (see §3) | open |
| S-005 | `recreate-manager.sh` | **defect** (tracked since the maintenance audit): always the source compose file, never `compose.image.yml` | AU3 reading | prepared-image installs recreate the wrong image | low | detect a prepared-image installation (`deploy/image.env`) | deploy | shell test | chunk 4 | closed in 1.30.41 |
| S-006 | `requirements.txt` paramiko | informational: OSV lists the SHA-1 RSA advisory for every 3.x/4.x release; devices may need SHA-1 | AU3 OSV query | none today | low | monitor; a major bump needs live device validation | device SSH | live | deferred (see §3) | open |
| T-001 | `docs/redesign/tools/verify_after.py` (`map-notes`) | **defect**: the Topology check still reads the `#map-notes` id removed in 1.30.36, so one check fails at every viewport | AU4 live run of the tool against the fixture manager (73/79) | the regression tool reports a false failure | high (tooling) | assert on the current markup (`#topology-hint`) | none | rerun the tool | chunk 3 | closed in 1.30.40 |
| T-002 | `docs/redesign/tools/verify_after.py` (Progress step) | **defect**: the tool clicks *Save progress* and never fills the mandatory "What changed?" label dialog (since 1.30.37), so the step times out and four later steps fail | AU4 live run; `docs/ui-ux-cleanup/tools/live_1_30_37_b.py` shows the working sequence | the regression tool cannot pass | high (tooling) | fill the label dialog like the newer tool | none | rerun the tool | chunk 3 | closed in 1.30.40 |
| T-003 | `restore.py` `_recheck_interrupted` (`LookupError` when the lab or candidate is gone) | test gap: no test triggers the branch; it is reported `uncertain` by the generic handler | AU4 grep | an untested failure branch of restart recovery | medium | add a unit test | none | `test_restore.py` | chunk 2 (backend) | closed in 1.30.42 |
| T-004 | `restore_junos.py`, `restore_eos.py` paste-prompt sleeps | hypothesis: unconditional real sleeps (about 15 s of suite time, no injection point); a slow device could race the prompt | AU4 timing | test speed; a live-timing risk without evidence | low | none without live evidence; a settle-parameter would need live validation | device behaviour | live | deferred (see §3) | open |
| T-005 | `tests/test_operations_ssh.py` 6 MiB stderr transfer | preference: the slowest test (14 s) over a real transport | AU4 timing | suite time | low | optional smaller payload | none | the test | deferred (see §3) | open |
| D-001 | `docs/FRESH-VM-GUIDE-V2.md`, `docs/WIKI-MASTER-GUIDE.md` | **defect**: "vJunos-switch cannot run inside a VM" contradicts the project's own live record (it runs on the dev VM with nested virtualisation, releases 1.28.0 to 1.30.39) | AU4 cross-reference; this audit's live lab | misleads readers | high | reword: needs nested virtualisation on the hypervisor | none | link check | chunk 3 | closed in 1.30.40 |
| D-002 | `docs/WIKI-MASTER-GUIDE.md` "Edit the map and export" | **defect**: describes the pre-builder basic editor; the current controls are missing | AU4 comparison with `map-editor.html` and LAB-OPERATIONS.md | wrong instructions | medium | rewrite from LAB-OPERATIONS.md "The map and its editor" | none | link check | chunk 3 | closed in 1.30.40 |
| D-003 | `deploy/compose.capture.yml` `CAPTURE_BIND`/`CAPTURE_PORT`, `setup_capture.py` | hypothesis (carried over): the two variables exist but the setup writes fixed loopback URLs | AU4 static check | a changed variable silently ignored | low | decide: drop the variables or honour them in the setup | deploy | `test_capture*.py`, capture smoke | chunk 4 | closed in 1.30.41 |
| D-004 | `docs/student-quick-start/tools/capture_scenario_b.py` | **defect**: references `#builder-yaml-text`/`#builder-yaml-dialog`, which no longer exist | AU4 static comparison with `lab-builder-yaml.js` | the guide's capture tool fails at the YAML step | medium | update the selectors | none | a replay | deferred: the student quick-start stream is excluded from this audit by the brief (recorded, not changed) | open |
| RR-201 | `runner.py` jobs trim (from the risk review of chunk 3's first draft) | **defect** in the first draft: a pending Git save's capture (`backup_job_id`) or a busy restore's backups could be evicted by the cap; a 1-minute schedule reaches 500 jobs in about 8 hours | reviewer's `rr2mod.py` with the real `submit()` | a pending save could never be retried | must-fix | protect every job id a pending save or a busy/interrupted restore references; make the cap per lab (300) | none | new tests | chunk 2 (backend) | closed in 1.30.42 |
| RR-202 | `lab_operations.py` `OutputWindow` carry-over | should-fix: the fixed carry-over withholds the newest complete lines from the live log while the helper is quiet (61 of 120 lines shown with a stored key) | reviewer's `rr2execmod.py` through the real `execute()` | the student cannot see what containerlab waits for | should-fix | hold back only a tail that is a proper prefix of a secret | none | lag and fuzz tests | chunk 2 (backend) | closed in 1.30.42 |
| RR-203 | `runner.py` git timeout branch | should-fix: a git process killed by the timeout can leave `.git/index.lock`, and every later backup of the lab ends `partial` | reviewer's `git_lock.py` | permanent degraded backups until a manual fix | should-fix | remove `index.lock`/`config.lock` in the timeout branch (single writer) | none | test with a planted lock | chunk 2 (backend) | closed in 1.30.42 |
| RR-204 | `restore.py` `_candidates` kept in finished jobs | should-fix: 200 restores of four 30 KiB configurations hold 50 MB in the state (334 ms per save) | reviewer's `restore_size.py` | the state growth B-001 set out to bound | should-fix | drop `_candidates` in `_finalize()` | none | test | chunk 2 (backend) | closed in 1.30.42 |
| RR-205 | `git_progress.py` `finish()` `unchanged` check | should-fix: after 200 later Git jobs the last uploaded save of a binding is gone and an unchanged save becomes `review_pending` | reviewer's `rr2mod.py` | needless reviews; blocked folder moves | should-fix | protect the newest pushed entry per binding digest | none | test | chunk 2 (backend) | closed in 1.30.42 |
| RR-206 | `main.py` `/api/state` read slices | note: the read slices (`[-200:]`) hide the protected older entries the trim keeps in front | reviewer's `rr2mod.py` | a pending save missing from the summaries | should-fix | drop the read slices now that storage is bounded at write | none | `test_app.py` | chunk 2 (backend) | closed in 1.30.42 |
| RR-207 | `NODE-FEATURES.md` cap sentence; `restore.py` comment; T-003 coverage | note: wording and a missing "lab gone" branch test | reviewer | misleading text | low | reword; extend the test | none | | chunk 2 (backend) | closed in 1.30.42 |
| R-001 | `deploy/install-manager.py`, `check_install.py`, `start-manager.sh`, docs | the telemetry retirement (see TELEMETRY-REMOVAL.md) | this audit | intentional removal | n/a | done | migration | see VALIDATION.md | lead | chunk 1 |

## 3. Remaining debt

Filled in at the end with the concrete constraint, impact, evidence and next action of each deferred item.
